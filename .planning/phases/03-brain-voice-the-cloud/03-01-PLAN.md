---
phase: 03-brain-voice-the-cloud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - daemon/src/brain/ClaudeBrain.ts
  - daemon/src/brain/ClaudeBrain.test.ts
  - daemon/src/brain/LocalBrain.ts
  - daemon/src/brain/LocalBrain.test.ts
  - daemon/src/brain/ClaudeCodeBrain.ts
  - daemon/src/brain/ClaudeCodeBrain.test.ts
  - daemon/src/brain/helper.ts
  - daemon/src/brain/helper.test.ts
  - daemon/src/brain/decision.ts
  - daemon/src/ipc/cues.ts
  - daemon/src/ipc/cues.test.ts
  - daemon/src/ipc/protocol.ts
  - daemon/src/ipc/protocol.test.ts
  - daemon/src/settings.ts
  - daemon/src/settings.test.ts
  - daemon/src/loop.ts
  - daemon/test/skeleton.e2e.test.ts
autonomous: true
requirements: [BRAIN-02, BRAIN-03, BRAIN-04, BRAIN-05, BRAIN-06, VOICE-02, CLOUD-04, CLOUD-01]
user_setup:
  - service: anthropic
    why: "ClaudeBrain (default) + ClaudeCodeBrain --bare auth"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> API Keys"
  - service: ollama
    why: "LocalBrain + always-on 7B helper (absent on this machine — degrades to typed escalation; live check is a manual owner step)"
    env_vars: []

must_haves:
  truths:
    - "ClaudeBrain (claude-opus-4-8) is the default brain and maps a reply to Decision.reply"
    - "On stop_reason 'tool_use', ClaudeBrain returns exactly ONE Decision.action (it does NOT execute the tool) so the loop's dispatch->gate.authorize stays the chokepoint"
    - "LocalBrain POSTs Ollama /api/chat and parses message.content -> Decision; Ollama-absent (ECONNREFUSED) returns a typed escalation, never throws across the loop boundary"
    - "ClaudeCodeBrain spawns `claude -p --output-format json --bare` and parses .result -> Decision.reply, restricted Green/Yellow-only"
    - "The local 7B helper (triage/classify/narrate) returns a neutral default when Ollama is absent and never blocks the loop"
    - "The cue assembler turns a reply + planned widget sequence into a SpeakSchema-valid frame with character-offset cues; the daemon ships all cues up front and NEVER sends timing"
    - "Settings brain=cloud|local swaps the active brain via the existing loop.setBrain seam over an additive IPC frame; the 7B helper runs regardless"
  artifacts:
    - path: "daemon/src/brain/ClaudeBrain.ts"
      provides: "Default brain via @anthropic-ai/sdk with a MANUAL tool loop and a test injection seam"
      contains: "claude-opus-4-8"
    - path: "daemon/src/brain/LocalBrain.ts"
      provides: "Ollama /api/chat brain, absent-tolerant"
      contains: "qwen2.5:7b-instruct-q4_K_M"
    - path: "daemon/src/brain/ClaudeCodeBrain.ts"
      provides: "Headless claude CLI brain (Green/Yellow only)"
    - path: "daemon/src/brain/helper.ts"
      provides: "Always-on 7B helper beside the providers"
    - path: "daemon/src/ipc/cues.ts"
      provides: "reply + widget plan -> SpeakSchema frame (char-offset cues)"
    - path: "daemon/src/settings.ts"
      provides: "brain=cloud|local state applied via setBrain"
    - path: "daemon/src/ipc/protocol.ts"
      provides: "Additive settings + ui.state frame arms"
      contains: "ui.state"
  key_links:
    - from: "daemon/src/brain/ClaudeBrain.ts"
      to: "loop.ts dispatch -> gate.authorize"
      via: "returns Decision.action; the loop gates+executes (never the SDK auto-runner)"
      pattern: "stop_reason"
    - from: "daemon/src/settings.ts"
      to: "loop.setBrain"
      via: "brain swap"
      pattern: "setBrain"
    - from: "daemon/src/ipc/cues.ts"
      to: "SpeakSchema"
      via: "validate before send"
      pattern: "SpeakSchema|safeParse"
---

<objective>
Fill the four brain seams Phases 1-2 left open and add the daemon-side choreography producer, all behind the EXISTING swap-seam. Implement ClaudeBrain (default, claude-opus-4-8, manual tool loop), LocalBrain (Ollama, absent-tolerant), ClaudeCodeBrain (headless `claude -p`, Green/Yellow-only), the always-on 7B helper, the cue assembler (`ipc/cues.ts`), the brain=cloud|local Settings path, and the additive IPC frame arms — without rewriting loop.ts/router/gate.

Purpose: closes talk->reason->respond on the daemon side and produces the `speak` frame the Face will choreograph against. This is the daemon half of the highest-risk phase.
Output: new brain modules + cue assembler + settings + additive protocol arms + unit tests + an extended e2e proving utterance -> mock-ClaudeBrain -> reply + speak frame over IPC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-brain-voice-the-cloud/03-RESEARCH.md
@.planning/phases/03-brain-voice-the-cloud/03-VALIDATION.md

# Frozen contracts and seams this plan builds against (read before writing code):
@daemon/src/brain/BrainProvider.ts
@daemon/src/brain/StubBrain.ts
@daemon/src/loop.ts
@daemon/src/tools/registry.ts
@daemon/src/ipc/protocol.ts
@daemon/src/ipc/server.ts
@daemon/src/tools/peekaboo.ts
@daemon/test/skeleton.e2e.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 + RED): brain test scaffolds, decision parser, additive IPC arms</name>
  <files>daemon/src/brain/decision.ts, daemon/src/brain/ClaudeBrain.test.ts, daemon/src/brain/LocalBrain.test.ts, daemon/src/brain/ClaudeCodeBrain.test.ts, daemon/src/brain/helper.test.ts, daemon/src/ipc/cues.test.ts, daemon/src/settings.test.ts, daemon/src/ipc/protocol.ts, daemon/src/ipc/protocol.test.ts</files>
  <behavior>
    - decision.ts: parseDecision(raw) JSON.parses then DecisionSchema.safeParse; on failure returns { thought:'unparseable brain output', reply: raw.slice(0,500) } (reuse existing DecisionSchema from BrainProvider.ts — do not redefine it).
    - protocol.ts (ADDITIVE only): add SettingsSchema { type:'settings', brain: enum['cloud','local'] } (Face->daemon) and UiStateSchema { type:'ui.state', state: enum['fullscreen','cornerPill','idle'] } (daemon->Face); append both to the FrameSchema discriminatedUnion; export their z.infer types. NEVER mutate existing arms (the Swift Face mirrors them).
    - protocol.test.ts (extend): assert a settings frame and a ui.state frame each round-trip through FrameSchema.safeParse; assert a speak frame carrying cues[] round-trips (the SpeakSchema is already frozen).
    - The five test files below are RED scaffolds asserting the behavior the later tasks make pass. Each must import the module-under-test (which does not exist yet) so the file fails to import until its task lands.
    - ClaudeBrain.test.ts: with the SDK client mocked via the injection seam — (a) a text reply maps to Decision.reply; (b) a stop_reason 'tool_use' reply maps to ONE Decision.action {tool,args} and NO tool is executed inside reason(); model string is 'claude-opus-4-8'.
    - LocalBrain.test.ts: with fetch mocked — (a) a JSON message.content maps to a Decision; (b) a rejected fetch (ECONNREFUSED) returns a Decision with a typed escalation reply, no throw; (c) a non-ok body naming a missing model surfaces an "ollama pull qwen2.5:7b-instruct-q4_K_M" escalation.
    - ClaudeCodeBrain.test.ts: with spawn/runner mocked — stdout `{"result":"...","session_id":"..."}` parses to Decision.reply; a non-zero/garbled stdout returns a typed escalation, no throw.
    - helper.test.ts: triage()/classify() return a neutral default when the Ollama call rejects, and never throw.
    - cues.test.ts: assembleSpeak(id, reply, widgetPlan) returns a frame that passes SpeakSchema.safeParse, with cues sorted by ascending atChar, each atChar within [0, reply.length], and onFinish dissolving the last presented widget.
    - settings.test.ts: applySettings('local') calls setBrain with a LocalBrain; applySettings('cloud') calls setBrain with a ClaudeBrain; the 7B helper is unaffected by the toggle.
  </behavior>
  <action>Create decision.ts importing DecisionSchema from ./BrainProvider.js (do NOT redefine the schema). Extend protocol.ts ADDITIVELY with SettingsSchema and UiStateSchema per RESEARCH.md "Code Examples — Extending the frozen FrameSchema additively"; append to the discriminatedUnion; export types. Write the six/seven test files as RED scaffolds (they import not-yet-existing modules and define the assertions above). Inject brain mocks the same way peekaboo.ts does it — add a `__setClientForTest`-style seam to ClaudeBrain (Task 2 implements it; the test references it now). Run the suite to confirm the new files are RED for the right reason (missing modules), not syntax errors in protocol.ts.</action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npx tsx --test src/ipc/protocol.test.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>protocol.test.ts is GREEN (settings/ui.state/speak frames round-trip); `npm run build` compiles protocol.ts + decision.ts; the five brain/cue/settings test files exist and fail only because their target modules are not yet created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): brains + helper + manual tool loop (BRAIN-02..06)</name>
  <files>daemon/src/brain/ClaudeBrain.ts, daemon/src/brain/LocalBrain.ts, daemon/src/brain/ClaudeCodeBrain.ts, daemon/src/brain/helper.ts</files>
  <behavior>
    - All four make their Wave-0 RED tests GREEN.
    - ClaudeBrain: implements BrainProvider; uses @anthropic-ai/sdk messages.create with model 'claude-opus-4-8' (per BRAIN-02); MANUAL tool loop (per BRAIN-06): on stop_reason 'tool_use' return ONE Decision.action {tool: tu.name, args: tu.input}; the loop gates+executes it. NEVER call the SDK auto Tool Runner / toolRunner / runTools.
    - LocalBrain: implements BrainProvider via native fetch to http://localhost:11434/api/chat with model qwen2.5:7b-instruct-q4_K_M, format:'json', no keep_alive:-1 (per BRAIN-03). Absent-tolerant per the peekaboo probe-then-escalate pattern.
    - ClaudeCodeBrain: spawns `claude -p <prompt> --output-format json --bare` with `--permission-mode dontAsk` (or `--allowedTools "Read"`) so it is Green/Yellow-only (per BRAIN-04, Red shim deferred to Phase 4); parses JSON.parse(stdout).result. Absent-tolerant.
    - helper.ts: standalone triage()/classify()/narrate() that always hit Ollama and return neutral defaults on absence (per BRAIN-05); NOT a BrainProvider, NOT swapped.
  </behavior>
  <action>Implement the four modules per RESEARCH.md Patterns 1-4 and the Code Examples. ClaudeBrain: add a `__setClientForTest(mock)` seam mirroring peekaboo.ts so Task-1 tests inject a mock SDK client; the manual loop returns one action per turn and defers execution to KERNEL's gated dispatch (BRAIN-06 — do NOT execute tools inside reason()). LocalBrain/ClaudeCodeBrain: probe-then-escalate (a rejected fetch / spawn ENOENT / non-zero exit -> typed escalation Decision, never throw across the loop boundary). For spawning `claude`, use node:child_process (zero-dep) OR execa — Claude's discretion; if execa is chosen, gate its install behind the legitimacy checkpoint (Task 0 of threat_model). Use parseDecision from decision.ts where a brain returns JSON text. Keep the model tags in named constants (RESEARCH A1: configurable, not buried).</action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npx tsx --test src/brain/ClaudeBrain.test.ts src/brain/LocalBrain.test.ts src/brain/ClaudeCodeBrain.test.ts src/brain/helper.test.ts && grep -RnE 'toolRunner|runTools|betaToolRunner' src/brain/ | grep -v '^#' | grep -c . | grep -qx 0 && echo "NO-AUTO-RUNNER-OK"</automated>
  </verify>
  <done>All four brain test files GREEN. ClaudeBrain's tool_use path returns a Decision.action and executes no tool inside reason(). LocalBrain/ClaudeCodeBrain return typed escalations when their dependency is absent. The grep gate confirms zero auto-tool-runner references in src/brain/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): cue assembler, settings/brain-swap wiring, e2e (CLOUD-04, VOICE-02, BRAIN-06)</name>
  <files>daemon/src/ipc/cues.ts, daemon/src/settings.ts, daemon/src/loop.ts, daemon/test/skeleton.e2e.test.ts</files>
  <behavior>
    - cues.ts: assembleSpeak(id, reply, widgetPlan) -> a SpeakSchema-valid `speak` frame keyed to CHARACTER OFFSETS in reply, cues sorted ascending by atChar, onFinish dissolving the last presented widget. Ships all cues up front; NEVER emits timing (per CLOUD-04 + ARCHITECTURE choreography contract). Makes cues.test.ts GREEN.
    - settings.ts: applySettings(brain:'cloud'|'local') calls setBrain(new LocalBrain()) or setBrain(new ClaudeBrain()); the 7B helper runs regardless of the toggle (per BRAIN-03/BRAIN-05). Makes settings.test.ts GREEN.
    - loop.ts (ADDITIVE wiring only — do NOT change the queue/drain/gate semantics): when a Decision carries a reply AND the originating intent is a user utterance, the reply is surfaced as today; the brain default remains StubBrain unless settings selected cloud — the actual brain swap happens via setBrain in settings.ts, not by editing drain(). If a reply path benefits from emitting a speak frame, add an OPTIONAL hook the IPC server can use; keep dispatch->gate.authorize untouched (BRAIN-06).
    - skeleton.e2e.test.ts (extend): a NEW test case — inject a mock ClaudeBrain via setBrain, send an `utterance`, assert a `reply` frame returns AND (where a widget plan is present) a `speak` frame validating against SpeakSchema is producible from the reply via assembleSpeak; assert the tool_use path reaches dispatch (spy) so the gate stays the chokepoint.
  </behavior>
  <action>Implement cues.ts and settings.ts to GREEN their tests. Wire settings into the IPC path: the server's frame handler for a `settings` frame calls applySettings; do this with the smallest additive change (extend defaultFrameHandler's switch additively — do not touch the utterance/ping arms' semantics). Extend the e2e to prove utterance -> (mock) ClaudeBrain -> reply over IPC and that a Decision.action reaches dispatch (BRAIN-06 anti-bypass). Do NOT rewrite drain()/dispatch()/authorize(). Keep the existing Phase-1 e2e case intact and GREEN.</action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npx tsx --test src/ipc/cues.test.ts src/settings.test.ts && npm test 2>&1 | tail -8</automated>
  </verify>
  <done>cues.test.ts + settings.test.ts GREEN; the full daemon suite (`npm test`) is GREEN including the extended e2e (utterance -> mock ClaudeBrain -> reply + speak-frame producible, tool_use reaches dispatch); no regression in the Phase-1/Phase-2 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Face -> daemon (UDS NDJSON) | Untrusted frames (settings, utterance) cross here; `safeParse` per line already enforced in server.ts |
| Claude API / Ollama / claude CLI -> daemon | Model output (untrusted text, may carry prompt-injected tool requests) crosses into the daemon |
| daemon decide -> act | Decision.action crosses into the gated router; the gate must stay physically between decide and act |
| package manager -> daemon | A new dependency (only if `execa` is chosen) crosses the supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | EoP | ClaudeBrain tool loop | mitigate | MANUAL loop only (BRAIN-06): reason() returns ONE Decision.action; the loop's dispatch->gate.authorize executes it. NO SDK auto-runner. Task-2 grep gate proves zero toolRunner refs; e2e proves an action reaches dispatch. |
| T-03-02 | Tampering/EoP | Prompt-injected tool call from model output | mitigate | Model output stays data; a tool request becomes a Decision.action that MUST pass gate.authorize (Phase-2 lock: Red = deny + escalate). No new external-content->tool path added; loop.ts dispatch chokepoint untouched. |
| T-03-03 | Info disclosure | ANTHROPIC_API_KEY in logs / memory repo | mitigate | Key read from env only; never logged by pino, never written to kernel-memory/. ClaudeCodeBrain `--bare` reads the env key, not keychain. |
| T-03-04 | DoS | Untrusted IPC frame (new settings/ui.state arms) crashing the daemon | mitigate | New arms added to the discriminatedUnion only; server.ts safeParse-per-line + error-frame property preserved (proven by protocol.test round-trip). |
| T-03-05 | EoP | ClaudeCodeBrain ambient money/irreversible rights | accept (this phase) | Green/Yellow-only via `--permission-mode dontAsk`/`--allowedTools`; the Red re-submission shim is deferred to Phase 4 (CC-03). Documented, low risk this phase. |
| T-03-SC | Tampering | npm install (only if `execa` chosen) | mitigate | slopcheck + a blocking-human checkpoint before install; `execa` is `[ASSUMED]` until slopcheck passes. node:child_process (zero-dep) is the default fallback that needs no install. |
</threat_model>

<verification>
- `cd daemon && npm test` GREEN (full suite, including the extended e2e).
- `cd daemon && npm run build` compiles (additive protocol arms typecheck; the Face mirrors these).
- grep gate: zero auto-tool-runner references in `src/brain/` (BRAIN-06).
- Manual owner checks (documented, NOT automated this session): live Ollama reply after `ollama pull qwen2.5:7b-instruct-q4_K_M`; a real ClaudeBrain reply with ANTHROPIC_API_KEY set; a real `claude -p` run for ClaudeCodeBrain.
</verification>

<success_criteria>
- ClaudeBrain default (claude-opus-4-8), manual tool loop, returns one action per turn (BRAIN-02, BRAIN-06).
- LocalBrain Ollama /api/chat, selectable from Settings, absent-tolerant (BRAIN-03).
- ClaudeCodeBrain headless `claude -p`, Green/Yellow-only (BRAIN-04).
- Always-on 7B helper, neutral default on absence (BRAIN-05).
- Cue assembler -> SpeakSchema frame, char-offset cues, no timing (CLOUD-04).
- Additive settings + ui.state frames round-trip (CLOUD-01 contract surface).
- utterance -> brain -> reply over IPC, action reaches the gate (VOICE-02, BRAIN-06).
</success_criteria>

<output>
Create `.planning/phases/03-brain-voice-the-cloud/03-01-SUMMARY.md` when done.
</output>
