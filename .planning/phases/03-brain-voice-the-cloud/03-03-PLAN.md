---
phase: 03-brain-voice-the-cloud
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - face/Kernel.xcodeproj
  - face/Kernel/KernelApp.swift
  - face/Kernel/Info.plist
  - face/Kernel/Kernel.entitlements
  - face/Spike/BoundarySpike.swift
  - face/KernelTests/BootstrapTests.swift
  - face/SPIKE-VERDICT.md
autonomous: false
requirements: [VOICE-03, CLOUD-01]
user_setup:
  - service: microphone-tcc
    why: "The boundary spike speaks aloud (no mic needed to SPEAK), but the Face app declares NSMicrophoneUsageDescription now so TCC identity is stable from the first signed build (Pitfall 4)."
    env_vars: []
  - service: avspeech-voice
    why: "The spike runs on-device against the target macOS 26 voice — owner runs it and records the verdict (ROADMAP criterion 2 mandates the spike precede the Stage)."
    env_vars: []

must_haves:
  truths:
    - "An Xcode project (face/Kernel.xcodeproj), NOT pure SwiftPM, exists with a stable bundle id, Info.plist (LSUIElement=YES, NSMicrophoneUsageDescription) and a mic entitlement so TCC grants survive rebuilds (Pitfall 4)"
    - "`xcodebuild -scheme Kernel -destination 'platform=macOS' build` compiles against the macOS 26.5 SDK (the only SDK present)"
    - "An XCTest target (KernelTests) builds and runs at least one passing test"
    - "The on-device boundary spike retains the AVSpeechSynthesizer as a PROPERTY (never a local var) and logs every willSpeakRangeOfSpeechString range for a fixed sentence containing a number"
    - "The spike verdict (do callbacks fire? do ranges drift on numbers?) is recorded in SPIKE-VERDICT.md and gates the Stage design in 03-04"
  artifacts:
    - path: "face/Kernel.xcodeproj"
      provides: "The Face Xcode project (TCC-stable identity, Info.plist, entitlements)"
    - path: "face/Kernel/KernelApp.swift"
      provides: "@main App + MenuBarExtra shell (CLOUD-01 surface; full cloud lands in 03-04)"
    - path: "face/Spike/BoundarySpike.swift"
      provides: "Throwaway on-device willSpeakRangeOfSpeechString spike (retained synth property)"
    - path: "face/SPIKE-VERDICT.md"
      provides: "Recorded spike verdict that gates the Stage build"
    - path: "face/KernelTests/BootstrapTests.swift"
      provides: "A building, passing XCTest proving the test target works"
  key_links:
    - from: "face/Spike/BoundarySpike.swift"
      to: "AVSpeechSynthesizerDelegate.willSpeakRangeOfSpeechString"
      via: "retained synth property + delegate"
      pattern: "willSpeakRange"
    - from: "face/Kernel/Info.plist"
      to: "TCC mic permanence"
      via: "NSMicrophoneUsageDescription + LSUIElement"
      pattern: "NSMicrophoneUsageDescription"
---

<objective>
Bootstrap the Face as an Xcode project (NOT pure SwiftPM — for TCC permanence + Info.plist/entitlements) and run the MANDATED on-device TTS-boundary spike FIRST, before any Stage code. The spike is a tiny throwaway that retains an AVSpeechSynthesizer as a property and logs every willSpeakRangeOfSpeechString range for a fixed sentence (including a number) on the target macOS 26 voice. Its verdict — do callbacks fire? do ranges drift? — is recorded and gates the dual-paced Stage build in 03-04.

This plan has no hard code dependency (it is Face-side bootstrap), so it runs in Wave 1 alongside the daemon plans; the spike-before-Stage ordering is enforced by 03-04's `depends_on: [03-01, 03-03]`.

Purpose: de-risks the phase's single highest-risk API (ROADMAP criterion 2 requires the spike precede the Stage) and establishes the TCC-stable signed identity the whole Face depends on (Pitfall 4).
Output: face/Kernel.xcodeproj (compiles), a MenuBarExtra app shell, Info.plist + entitlements, a building XCTest target, the BoundarySpike, and SPIKE-VERDICT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-brain-voice-the-cloud/03-RESEARCH.md
@.planning/phases/03-brain-voice-the-cloud/03-UI-SPEC.md
@.planning/phases/03-brain-voice-the-cloud/03-VALIDATION.md

# The frozen frame contract the Face mirrors (read for the eventual NWConnection client in 03-04):
@daemon/src/ipc/protocol.ts
@daemon/src/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Xcode project bootstrap + MenuBarExtra shell + XCTest target</name>
  <files>face/Kernel.xcodeproj, face/Kernel/KernelApp.swift, face/Kernel/Info.plist, face/Kernel/Kernel.entitlements, face/KernelTests/BootstrapTests.swift</files>
  <action>Create face/Kernel.xcodeproj as a macOS app (NOT pure SwiftPM — RESEARCH Open Question 1 / Pattern 7: Info.plist + entitlements + stable signed identity for TCC permanence, Pitfall 4). Set a stable bundle id (e.g. com.kernel.face), a conservative deployment target (macOS 14), build against the macOS 26.5 SDK (the only SDK present — RESEARCH Toolchain note). KernelApp.swift: @main App with a MenuBarExtra (`.menuBarExtraStyle(.window)`) — the menubar presence for CLOUD-01; the full cloud window + SMAppService launch-at-login land in 03-04 (this is the shell). Info.plist: LSUIElement=YES (menubar-only, no Dock icon), NSMicrophoneUsageDescription (a terse usage string — declared NOW so the TCC identity is stable from the first build even though the spike does not need the mic). Kernel.entitlements: microphone device entitlement; start NON-SANDBOXED (Developer-ID, not App Store — RESEARCH Open Question 4). Add a KernelTests XCTest target with BootstrapTests.swift containing one trivial passing test (e.g. asserts the app bundle id constant) so the test lane is proven to build+run.</action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/face && xcodebuild -scheme Kernel -destination 'platform=macOS' build 2>&1 | tail -3 && xcodebuild test -scheme Kernel -destination 'platform=macOS' 2>&1 | tail -5</automated>
  </verify>
  <done>`xcodebuild build` succeeds against the macOS 26.5 SDK; `xcodebuild test` runs and BootstrapTests passes; Info.plist has LSUIElement=YES + NSMicrophoneUsageDescription; the app is a menubar (LSUIElement) Xcode project, not SwiftPM.</done>
</task>

<task type="auto">
  <name>Task 2: the on-device willSpeakRangeOfSpeechString boundary spike</name>
  <files>face/Spike/BoundarySpike.swift</files>
  <action>Create a throwaway spike (RESEARCH "lynchpin discipline" + Pitfall 1 + ROADMAP criterion 2). A small type that owns an AVSpeechSynthesizer as a RETAINED PROPERTY (never a local var — the #1 cause of the delegate never firing, Apple Forums 683471), conforms to AVSpeechSynthesizerDelegate, speaks a FIXED sentence that contains a number (e.g. a sentence with "2020" / "twelve hundred") on the target macOS 26 system voice, and logs every willSpeakRangeOfSpeechString NSRange (location+length+the substring) plus didFinish. Expose it so the owner can trigger it from the running app (e.g. a menubar menu item "Run boundary spike" or a debug button). It must NOT crash on an out-of-bounds NSRange (guard the substring extraction) — that resilience is itself part of what the spike validates. Keep it isolated under face/Spike/ so it is trivially deletable after the verdict is recorded.</action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/face && xcodebuild -scheme Kernel -destination 'platform=macOS' build 2>&1 | tail -3 && grep -q 'willSpeakRange' face/Spike/BoundarySpike.swift && grep -qiE 'let .*synth|var .*synth' face/Spike/BoundarySpike.swift && echo SPIKE-WIRED</automated>
  </verify>
  <done>The spike compiles into the app, retains the synthesizer as a property, implements willSpeakRangeOfSpeechString, speaks a number-containing sentence, logs every range, and guards against an out-of-bounds NSRange. Owner-triggerable from the running app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>An on-device boundary spike: the Face speaks a fixed number-containing sentence on the target macOS 26 voice and logs every `willSpeakRangeOfSpeechString` range. This gates the Stage design in 03-04 (ROADMAP criterion 2 mandates the spike precede the Stage build).</what-built>
  <how-to-verify>
    1. Build + run the Face: `cd face && xcodebuild -scheme Kernel -destination 'platform=macOS' build`, then launch the produced .app (or run from Xcode).
    2. Trigger "Run boundary spike" from the menubar/debug control. Listen — the fixed sentence is spoken.
    3. Watch the console/log: does `willSpeakRangeOfSpeechString` fire at all? Roughly once per word? Do the ranges land ON the right substrings, or drift (especially around the number)?
    4. Record the answers in face/SPIKE-VERDICT.md: (a) callbacks fire yes/no; (b) ranges accurate vs drift-on-numbers; (c) approximate per-character timing if measurable (calibrates the sentence-time fallback); (d) verdict — is word-level callback pacing usable as PRIMARY, or must the sentence-time fallback be PRIMARY on this OS+voice?
  </how-to-verify>
  <resume-signal>Type "approved" once SPIKE-VERDICT.md is written, or describe what the spike showed so the Stage design (03-04) is built on the real verdict.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Face app -> macOS TCC | The signed app identity is the basis for mic permission permanence |
| (no daemon IPC yet) | This plan is bootstrap + spike only; the NWConnection client lands in 03-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | DoS | TCC identity churn (mic silently fails after rebuild) | mitigate | Xcode project with a stable bundle id + signed identity + NSMicrophoneUsageDescription declared from the first build (Pitfall 4). Non-sandboxed Developer-ID. |
| T-03-10 | DoS | Out-of-bounds NSRange from AVSpeech crashing the Stage clock | mitigate | The spike itself guards substring extraction against an invalid range; the verdict informs the 03-04 Stage to fall through to the time-based path on a bad range. |
| T-03-11 | Info disclosure | spike speaks + logs locally only | accept | The spike only speaks + logs ranges locally; no data crosses a boundary. Mic RMS stays Face-local by design (CLOUD-03) — established in 03-04. |
</threat_model>

<verification>
- `cd face && xcodebuild -scheme Kernel -destination 'platform=macOS' build` compiles (macOS 26.5 SDK).
- `cd face && xcodebuild test -scheme Kernel` runs; BootstrapTests passes.
- Manual owner check (THE gating check): run the spike on-device, record SPIKE-VERDICT.md (callbacks fire? ranges drift? fallback PRIMARY or word-level PRIMARY?).
</verification>

<success_criteria>
- Face is an Xcode project (TCC-stable identity), compiles against macOS 26.5 SDK, menubar app (CLOUD-01 surface) (CLOUD-01).
- XCTest target builds + passes.
- The mandated on-device boundary spike runs with a RETAINED synth property and its verdict is recorded BEFORE the Stage build (VOICE-03, ROADMAP criterion 2).
</success_criteria>

<output>
Create `.planning/phases/03-brain-voice-the-cloud/03-03-SUMMARY.md` when done (include the spike verdict summary).
</output>
