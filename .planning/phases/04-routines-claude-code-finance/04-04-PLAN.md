---
phase: 04-routines-claude-code-finance
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - daemon/src/ipc/protocol.ts
  - daemon/src/ipc/protocol.test.ts
  - daemon/src/tools/claude-code.ts
  - daemon/src/tools/claude-code.test.ts
  - daemon/src/safety/gate.test.ts
  - kernel-memory/projects/registry.md
  - face/Kernel/IPC/Frames.swift
  - face/Kernel/ClaudeCode/TranscriptPill.swift
  - face/Kernel/AppCoordinator.swift
  - face/Kernel/CloudView/CloudWindow.swift
  - face/KernelTests/FrameCodecTests.swift
  - face/KernelTests/TranscriptPillTests.swift
autonomous: true
requirements: [CC-01, CC-02, CC-03, CC-04]
must_haves:
  truths:
    - "Prompts to Claude Code are authored in first person, as Pravin (CC-01)"
    - "claude -p --output-format stream-json --include-partial-messages NDJSON events become transcript frames over a NEW additive IPC arm (CC-02)"
    - "The shipped cornerPill renders a live, scrollable Kernel<->Claude transcript with a streaming pulse and a pause control (CC-02)"
    - "Every Claude Code project is appended to projects/registry.md for cold resume (CC-04)"
    - "A Red-tier action proposed by a Claude Code session is DENIED by the shipped gate; the re-submission shim stays deferred to Phase 5 — Green/Yellow only (CC-03)"
  artifacts:
    - path: "daemon/src/ipc/protocol.ts"
      provides: "additive TranscriptSchema arm appended to FrameSchema (existing arms untouched)"
      contains: "TranscriptSchema"
    - path: "daemon/src/tools/claude-code.ts"
      provides: "first-person prompt + stream-json runner -> transcript frames + registry append"
    - path: "face/Kernel/ClaudeCode/TranscriptPill.swift"
      provides: "scrollable transcript in the cornerPill with streaming pulse + pause"
    - path: "kernel-memory/projects/registry.md"
      provides: "the cold-resume project registry (append target)"
  key_links:
    - from: "daemon/src/tools/claude-code.ts"
      to: "daemon/src/ipc/protocol.ts"
      via: "push TranscriptSchema frames per stream-json event"
      pattern: "transcript"
    - from: "face/Kernel/IPC/Frames.swift"
      to: "daemon/src/ipc/protocol.ts"
      via: "byte-exact mirror of the new transcript arm"
      pattern: "transcript"
    - from: "face/Kernel/CloudView/CloudWindow.swift"
      to: "face/Kernel/ClaudeCode/TranscriptPill.swift"
      via: "cornerPill hosts the TranscriptPill"
      pattern: "TranscriptPill"
---

<objective>
Build the Claude Code bridge: author prompts in first person as Pravin, run `claude -p --output-format stream-json --include-partial-messages`, parse the NDJSON events into a NEW ADDITIVE `transcript` arm on the frozen `FrameSchema` (mirrored byte-exact in `Frames.swift`), and render them in the shipped `cornerPill` cloud state as a live, scrollable transcript with a streaming pulse + pause control. Every project is appended to `kernel-memory/projects/registry.md` for cold resume.

Per the locked decision and 04-RESEARCH Pitfall 6: Phase 4 is Green/Yellow ONLY. The runner keeps the shipped fence (`--permission-mode dontAsk --allowedTools Read`); any Red-tier action a session proposes hits the shipped `gate.authorize` and is DENIED. The Red re-submission shim is DEFERRED to Phase 5 — this plan proves the denial, it does not build the shim.

The IPC change is strictly additive (Pattern 1): append `TranscriptSchema` to the discriminatedUnion; existing arms are NEVER mutated. The Swift mirror adds one `case transcript(...)` with decode/encode arms; FrameCodecTests prove the round-trip.

Purpose: ROADMAP Phase-4 criterion 5 — first-person prompting, the transparency corner-pill, the project registry, Red-deny (shim deferred).
Output: the additive transcript arm (TS + Swift), `daemon/src/tools/claude-code.ts`, `TranscriptPill.swift`, AppCoordinator transcript handling, the registry, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-routines-claude-code-finance/04-RESEARCH.md
@.planning/phases/04-routines-claude-code-finance/04-UI-SPEC.md
@.planning/phases/04-routines-claude-code-finance/04-VALIDATION.md

# Shipped seams this plan composes (read-only except the additive arm — do NOT mutate existing arms):
@daemon/src/ipc/protocol.ts
@daemon/src/brain/ClaudeCodeBrain.ts
@daemon/src/safety/gate.ts
@daemon/src/safety/tiers.ts
@daemon/src/tools/registry.ts
@daemon/src/tools/Tool.ts
@face/Kernel/IPC/Frames.swift
@face/Kernel/CloudView/CloudWindow.swift
@face/Kernel/AppCoordinator.swift
@face/Kernel/DesignSystem/Tokens.swift
@face/KernelTests/FrameCodecTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive transcript IPC arm + first-person prompt + stream-json runner + registry (daemon)</name>
  <files>daemon/src/ipc/protocol.ts, daemon/src/ipc/protocol.test.ts, daemon/src/tools/claude-code.ts, daemon/src/tools/claude-code.test.ts, daemon/src/safety/gate.test.ts, kernel-memory/projects/registry.md</files>
  <behavior>
    - protocol.ts (CC-02): APPEND a TranscriptSchema { type:'transcript', id, role:'kernel'|'claude', text, partial?:boolean } and add it to the FrameSchema discriminatedUnion array. Existing arms (hello/utterance/.../ui.state/error) are byte-for-byte UNCHANGED. protocol.test.ts: a transcript frame safeParses; a malformed transcript frame is rejected; every previously-passing protocol test still passes (no mutation).
    - claude-code.ts (CC-01/02/04): authorFirstPersonPrompt(task) builds a prompt written AS Pravin ("I need you to ..." personal/direct register — assert first-person + no third-person "Kernel"/"the user"). runSession(task, deps) spawns `claude -p <prompt> --output-format stream-json --include-partial-messages --bare --permission-mode dontAsk --allowedTools Read` (Green/Yellow fence retained), reads stdout line-by-line (NDJSON), and per event pushes a TranscriptSchema frame {role:'claude', text, partial} via an injected emit(frame) seam. On session start it appends a row to projects/registry.md (CC-04). __setRunnerForTest injects a mock stream-json runner (mirror ClaudeCodeBrain.__setRunnerForTest) — NEVER spawn a real claude in tests.
    - gate.test.ts (CC-03): EXTEND — a Red-shaped ToolCall originating from a Claude Code session (e.g. op 'purchase' / 'rm -rf') classifies red and gate.authorize returns kind:'deny' with an escalation. Assert NO 'gated'/'allow' for Red (the shim is deferred; Green/Yellow only).
    claude-code.test.ts (CC-01/02/04): first-person prompt assertion; stream-json events → transcript frames (count + role + partial flag) via the mock runner; a partial then a final line; registry.md gains a row per session.
  </behavior>
  <action>
    Append TranscriptSchema to protocol.ts AND its FrameSchema union + a Transcript type export — DO NOT touch any existing arm (Pattern 1 / Anti-Pattern: mutating an arm). Implement claude-code.ts reusing the ClaudeCodeBrain spawn discipline (node:child_process, absent-tolerant, __setRunnerForTest) but with the stream-json argv + NDJSON line parsing; emit transcript frames through an injected seam (the IPC server supplies the real emitter; tests supply a capture). Append to projects/registry.md with explicit path writes (never git add -A near it). Extend gate.test.ts for the Red-from-CC denial (do NOT modify gate.ts — the shipped Red=deny already holds; this only adds coverage). Seed kernel-memory/projects/registry.md with a header if absent (the file is the append target, NOT finance-pathed).
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npx tsx --test src/ipc/protocol.test.ts src/tools/claude-code.test.ts src/safety/gate.test.ts</automated>
  </verify>
  <done>The additive transcript arm parses (existing arms untouched); first-person prompt authored; stream-json events become transcript frames via the mock runner; each session appends to registry.md; a Red-from-CC action is denied by the shipped gate. All three test files green.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the transcript arm in Frames.swift + FrameCodec round-trip (Face)</name>
  <files>face/Kernel/IPC/Frames.swift, face/KernelTests/FrameCodecTests.swift</files>
  <behavior>
    - Frames.swift: add one `case transcript(id: String, role: TranscriptRole, text: String, partial: Bool?)` to the Frame enum with its decode ("transcript") + encode arms, byte-exact to TranscriptSchema; add a `TranscriptRole: String, Codable { case kernel, claude }`; add the needed CodingKeys (role, partial — text/id already exist). Existing arms are UNCHANGED.
    - FrameCodecTests: extend the round-trip proof — a transcript frame encodes→decodes identically (kernel + claude roles, partial true/false/absent); an unknown type still returns nil (no crash). All existing FrameCodec tests still pass.
  </behavior>
  <action>
    Mirror the new arm exactly as the daemon's TranscriptSchema. Follow the existing Frames.swift discriminated-union pattern (decode narrows on `type`, encode re-emits `type` + the arm fields). Reuse the existing `id`/`text` CodingKeys; add `role`/`partial`. Do NOT mutate any existing case (the Swift mirror is byte-exact — a mutated arm silently breaks decode, T-03-13). Extend FrameCodecTests for the new arm.
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/face && xcodebuild -scheme Kernel -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test 2>&1 | tail -5</automated>
  </verify>
  <done>Frames.swift mirrors the transcript arm byte-exact; FrameCodecTests round-trips it for both roles + partial states; existing arms + tests unchanged and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TranscriptPill in the cornerPill + AppCoordinator transcript handling (Face)</name>
  <files>face/Kernel/ClaudeCode/TranscriptPill.swift, face/Kernel/AppCoordinator.swift, face/Kernel/CloudView/CloudWindow.swift, face/KernelTests/TranscriptPillTests.swift</files>
  <behavior>
    - AppCoordinator: handle the new `.transcript(id, role, text, partial)` inbound frame — append/merge lines into a @Published transcriptLines buffer (a partial chunk updates the in-progress line; a non-partial finalizes it); expose a paused flag toggled by a pause control. The existing handle(_:) switch gains a case; all other cases unchanged.
    - TranscriptPill (04-UI-SPEC §6, CC-02): a scrollable transcript view (newest at bottom, auto-scroll with manual scrollback) rendering Kernel↔Claude lines in monospace-ish Label text, an accent live-pulse dot while streaming, and a pause control. Renders ONLY the typed transcript text (no remote-resource loads).
    - CloudWindow.cornerPill: host the TranscriptPill inside the existing pill (the pill already exists for the Claude Code session state; add the transcript content). Do NOT touch widgetView(named:) (04-01 owns the four widget cases).
    - TranscriptPillTests: streamed events render in order; a partial line updates rather than duplicates; the pause control toggles paused; the streaming pulse shows while partial events arrive.
  </behavior>
  <action>
    Add the transcript case to AppCoordinator.handle and a transcriptLines buffer + paused flag (guarded under XCTest like the rest). Build TranscriptPill following Tokens/Motion and the EventsWidget defensive-render discipline. Wire it into CloudWindow.cornerPill (the pill body) — the pill already renders the mini-cloud + pulse; add the scrollable transcript. Author TranscriptPillTests. Add the new files to the Kernel + KernelTests targets. Do NOT modify Frames.swift (Task 2 did) or CloudWindow.widgetView (04-01 owns it).
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/face && xcodebuild -scheme Kernel -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test 2>&1 | tail -5</automated>
  </verify>
  <done>The cornerPill renders a live scrollable Kernel↔Claude transcript with a streaming pulse + pause; partial lines update in place; AppCoordinator buffers transcript frames. TranscriptPillTests green; full Face suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claude CLI stdout → daemon | stream-json NDJSON events are untrusted output parsed into transcript frames |
| Claude Code session → gate | any action a session proposes crosses the shipped gate.authorize |
| daemon transcript frames → Face | new additive arm; the Face renders typed text only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Elevation of Privilege | Claude Code ambient Red/irreversible action | mitigate | Green/Yellow-only fence (--permission-mode dontAsk --allowedTools Read); Red → gate.authorize deny; shim deferred to Phase 5 (CC-03) |
| T-04-18 | Tampering | mutating an existing FrameSchema arm | mitigate | strictly APPEND TranscriptSchema; Swift mirror byte-exact; FrameCodec + protocol tests prove existing arms unchanged (Anti-Pattern 1) |
| T-04-19 | Information Disclosure | transcript rendering a remote resource | mitigate | TranscriptPill renders ONLY typed transcript text — no AsyncImage/URLRequest/WKWebView (T-03-12) |
| T-04-20 | Tampering | malformed stream-json crashing the loop | mitigate | NDJSON parsed line-by-line, absent-tolerant (mirrors ClaudeCodeBrain); a bad line is dropped, never throws across the boundary |
| T-04-21 | Information Disclosure | registry.md picked up by backup | accept | registry.md is non-finance project metadata in the memory repo; finance/ stays gitignored; explicit-path writes only (no git add -A) |
</threat_model>

<verification>
- `cd daemon && npx tsx --test src/ipc/protocol.test.ts src/tools/claude-code.test.ts src/safety/gate.test.ts` green (CC-01/02/03/04 daemon).
- `cd face && xcodebuild -scheme Kernel -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test` green (Frames mirror + TranscriptPill).
- `cd daemon && npm test` stays ≥108 green.
- Additive-only guard: `git -C /Users/pravinmaurya/Documents/KernelAi diff daemon/src/ipc/protocol.ts | grep -E '^-' | grep -vE '^---'` shows NO removed lines from existing arms (append-only).
</verification>

<success_criteria>
- First-person prompts authored as Pravin (CC-01).
- stream-json events stream over a new additive transcript arm into the cornerPill, scrollable with a pause (CC-02).
- Every project appended to projects/registry.md (CC-04).
- A Red-from-CC action is denied; the re-submission shim stays deferred to Phase 5 (CC-03).
</success_criteria>

<output>
Create `.planning/phases/04-routines-claude-code-finance/04-04-SUMMARY.md` when done.
</output>
