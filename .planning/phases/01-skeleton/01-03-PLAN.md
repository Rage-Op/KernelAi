---
phase: 01-skeleton
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - daemon/src/ipc/protocol.ts
  - daemon/src/ipc/protocol.test.ts
  - daemon/src/ipc/server.ts
  - daemon/src/ipc/server.test.ts
  - daemon/src/memory/log.ts
  - daemon/src/memory/log.test.ts
  - daemon/src/loop.ts
  - daemon/src/loop.test.ts
  - daemon/src/heartbeat.ts
  - daemon/test/heartbeat.test.ts
  - daemon/src/index.ts
  - launchd/com.kernel.daemon.plist
  - launchd/com.kernel.heartbeat.plist
  - launchd/README.md
autonomous: false
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05]
user_setup: []

must_haves:
  truths:
    - "The daemon exposes a Unix-domain-socket NDJSON IPC endpoint, sends ready on connect, answers ping with pong, and the Face can attach/detach/re-attach without a daemon restart"
    - "An event-driven serial intent runner drains one intent at a time and falls genuinely idle (running=false, no timer) when the queue empties — no polling tick"
    - "One loop tick runs perceive→recall(inject)→decide(StubBrain)→act(no tool in P1)→log and pushes a reply frame"
    - "All daemon activity is appended (never truncated) as ## Session N blocks to the append-only logs/{date}.md under the memory repo"
    - "A short-lived --heartbeat invocation appends one dated heartbeat line to logs/{today}.md and exits"
    - "Two launchd plists (daemon RunAtLoad+KeepAlive, heartbeat StartCalendarInterval) load via launchctl bootstrap gui/$(id -u) and relaunch the daemon at login"
    - "The Walking-Skeleton end-to-end test (01-01 skeleton.e2e.test.ts) now passes green"
  artifacts:
    - path: "daemon/src/ipc/protocol.ts"
      provides: "Frozen zod-validated NDJSON frame contract (envelope + P1 frames + designed-for P2/P3 shapes)"
      exports: ["FrameSchema", "Envelope"]
    - path: "daemon/src/ipc/server.ts"
      provides: "UDS net.createServer + NDJSON buffered line framing + server→client push"
      exports: ["startIpc", "send"]
    - path: "daemon/src/loop.ts"
      provides: "Event-driven serial intent runner (enqueue + single drain)"
      exports: ["enqueue", "drain"]
    - path: "daemon/src/memory/log.ts"
      provides: "Append-only session-block + heartbeat writer to logs/{date}.md"
      exports: ["logSession", "logHeartbeat"]
    - path: "daemon/src/heartbeat.ts"
      provides: "Short-lived heartbeat: append a dated line, exit"
      exports: ["runHeartbeat"]
    - path: "daemon/src/index.ts"
      provides: "Entry: --heartbeat branch; startup guards (IDENTITY hash, finance ls-files); start ipc+loop"
    - path: "launchd/com.kernel.daemon.plist"
      provides: "Login agent: RunAtLoad + KeepAlive"
      contains: "RunAtLoad"
    - path: "launchd/com.kernel.heartbeat.plist"
      provides: "Timed heartbeat: StartCalendarInterval"
      contains: "StartCalendarInterval"
  key_links:
    - from: "daemon/src/ipc/server.ts"
      to: "daemon/src/loop.ts"
      via: "an utterance frame enqueues a user intent"
      pattern: "enqueue"
    - from: "daemon/src/loop.ts"
      to: "daemon/src/memory/inject.ts"
      via: "recall step calls inject()"
      pattern: "inject"
    - from: "daemon/src/loop.ts"
      to: "daemon/src/brain/StubBrain.ts"
      via: "decide step calls brain.reason()"
      pattern: "reason"
    - from: "daemon/src/loop.ts"
      to: "daemon/src/memory/log.ts"
      via: "log step appends a session block"
      pattern: "logSession"
    - from: "launchd/com.kernel.heartbeat.plist"
      to: "daemon/src/index.ts"
      via: "ProgramArguments runs node dist/index.js --heartbeat"
      pattern: "--heartbeat"
---

<objective>
Close the Walking Skeleton: the UDS NDJSON IPC server (frozen `protocol.ts` frame contract + buffered line framing), the event-driven serial intent runner (`loop.ts`), the append-only session-block + heartbeat log writer, the short-lived `--heartbeat` job, the daemon entry point with startup guards, and the two launchd plists (daemon login agent + timed heartbeat). This makes the full perceive→recall→decide→act→log tick run end to end and turns the failing `skeleton.e2e.test.ts` (from Plan 01) green.

Purpose: Make ROADMAP success criteria 1, 3, and 5 true — the daemon persists and relaunches at login, runs the event-driven loop and falls idle, the timed heartbeat writes a dated log entry, and the Face can attach over the UDS without a daemon restart. This is the final vertical slice; after it, a real client sends a frame and gets a reasoned reply backed by injected memory and a logged session.

Output: A running daemon, the loop, the IPC endpoint, the heartbeat, the launchd jobs, and a green end-to-end skeleton test plus a manual launchd phase-gate runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skeleton/01-RESEARCH.md
@.planning/phases/01-skeleton/01-VALIDATION.md
@.planning/phases/01-skeleton/01-01-SUMMARY.md
@.planning/phases/01-skeleton/01-02-SUMMARY.md
@daemon/src/config.ts
@daemon/src/brain/BrainProvider.ts
@daemon/src/brain/StubBrain.ts
@daemon/src/memory/inject.ts
@daemon/src/memory/identity.ts
@daemon/test/skeleton.e2e.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frozen IPC protocol + UDS NDJSON server (with partial-frame buffering) + append-only log writer</name>
  <files>daemon/src/ipc/protocol.ts, daemon/src/ipc/protocol.test.ts, daemon/src/ipc/server.ts, daemon/src/ipc/server.test.ts, daemon/src/memory/log.ts, daemon/src/memory/log.test.ts</files>
  <behavior>
    - protocol.ts FrameSchema (zod) validates the P1 frames: Face→daemon hello / utterance{id,text,final} / ping{id}; daemon→Face ready / reply{id,text} / pong{id} / error. The P2/P3 shapes (speak{cues,onFinish}, widget.data, ui.intent) are authored so the contract is stable but are not exercised in P1.
    - server.ts startIpc(onFrame) creates the UDS at the config socket path (mkdir the dir, unlink any stale socket), sends a ready frame on connect, buffers incoming bytes and splits on \n (carrying a partial trailing line across data events), JSON.parses each complete line, validates with FrameSchema, and calls onFrame(frame, conn). Malformed/invalid lines reply with an error frame WITHOUT crashing.
    - send(conn, frame) writes JSON.stringify(frame)+'\n' (server→client push is just a write).
    - A split JSON line delivered in two chunks yields exactly one parsed frame (partial-frame handling).
    - logSession({intent, decision}) APPENDS a ## Session N block to logs/{today}.md (never truncates); logHeartbeat() appends a dated heartbeat line. Re-running appends a new block, leaving prior content intact.
  </behavior>
  <action>
    Create `daemon/src/ipc/protocol.ts` per RESEARCH.md IPC Contract: a zod `FrameSchema` (discriminated union on `type`) covering P1 frames `hello`, `utterance` (`{id, text, final}`), `ping` (`{id}`), `ready`, `reply` (`{id, text}`), `pong` (`{id}`), `error` (`{id?, message}`). Author the P2/P3 frame shapes (`speak` with `cues[]`/`onFinish`, `widget.data`, `ui.intent`) as commented or optional members so the contract the Swift Face mirrors is stable now (transport-agnostic) — they are not used in P1. Export `FrameSchema` and the `Envelope` type.

    Create `daemon/src/ipc/server.ts` from RESEARCH.md Code Example (the one subtle hand-rolled component): `startIpc(onFrame)` resolves the socket path from `config`, `fs.mkdirSync(dir, {recursive:true})`, `try { fs.unlinkSync(SOCK) } catch {}`, `net.createServer((conn) => { send(conn, {type:'ready', daemon:'kernel', version:'0.1.0'}); ... })`. Maintain a per-connection string buffer; on `data`, append, loop `indexOf('\n')`, slice each complete line, keep the partial remainder, `JSON.parse` + `FrameSchema.safeParse` each line, call `onFrame` on success, `send` an `error` frame on failure (never crash). Export `send(conn, frame)`. Listen on the UDS and return the server.

    Create `daemon/src/memory/log.ts` (CORE-05 append-only): `logSession({intent, decision})` resolves `logs/{YYYY-MM-DD}.md` under the memory dir, determines the next `## Session N` number (count existing blocks), and `fs.appendFileSync` a block containing the intent, the decision `thought`, and `reply` plus an ISO timestamp — APPEND only, never truncate. `logHeartbeat()` appends a single `heartbeat {ISO timestamp}` line to the same dated file. Wire `pino` for structured JSON event logging alongside the markdown session blocks (the raw events later phases distill); `pino-pretty` is dev-only and must never be in the launchd-run path.

    Create the three matching `node:test` files. `protocol.test.ts`: valid frames parse, an unknown type / missing field is rejected. `server.test.ts`: start the server, connect a tiny Node UDS client, assert the `ready` frame arrives, send a `ping` and assert `pong`; AND feed a single JSON line split across two `conn.write` chunks and assert exactly one frame is parsed (partial-frame test). `log.test.ts`: call `logSession` twice against a temp memory dir, assert two `## Session` blocks exist and the first is still intact (append-only), and assert `logHeartbeat` adds a dated line.
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npx tsx --test src/ipc/protocol.test.ts src/ipc/server.test.ts src/memory/log.test.ts</automated>
  </verify>
  <done>UDS NDJSON server sends `ready`, answers `ping`→`pong`, handles partial frames, rejects malformed frames without crashing; the frozen `protocol.ts` contract validates P1 frames; `log.ts` appends `## Session N` blocks + heartbeat lines (never truncates). Tests pass. (CORE-04, CORE-05; VALIDATION: "Face can attach over UDS without restart", "NDJSON partial-frame handling", "Append-only event log".)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Event-driven serial intent runner + heartbeat + entry point with startup guards</name>
  <files>daemon/src/loop.ts, daemon/src/loop.test.ts, daemon/src/heartbeat.ts, daemon/test/heartbeat.test.ts, daemon/src/index.ts</files>
  <behavior>
    - enqueue(intent) pushes onto a queue and triggers drain(); drain() runs one intent at a time (single drain — running guard prevents concurrent passes) and, when the queue empties, sets running=false and returns (falls genuinely idle — NO setInterval, no timer left armed).
    - One drained intent runs: recall via inject(query) → decide via brain.reason() (StubBrain) → act (no tool in P1; decision.action is left undispatched) → log via logSession(); the reply text is pushed back to the originating connection.
    - After draining a fake intent: a ## Session block exists in logs/{today}.md AND a reply was produced; running===false and the queue is empty.
    - runHeartbeat() appends one dated heartbeat line to logs/{today}.md and resolves (the process then exits) — short-lived, no socket, no loop.
    - index.ts: if argv includes --heartbeat → runHeartbeat() then exit; else run startup guards (readIdentityVerified/baseline the IDENTITY hash; assert git ls-files finance is empty) then startIpc(onFrame) where onFrame enqueues a user intent for an utterance and replies for ping.
  </behavior>
  <action>
    Create `daemon/src/loop.ts` per RESEARCH.md Pattern 1 (event-driven serial intent runner — explicitly NOT a polling `setInterval`): module-level `queue: Intent[]` and `running` flag; `enqueue(intent)` pushes and calls `void drain()`; `drain()` returns early if `running`, else sets `running=true`, and `while (queue.length)` shifts an intent and runs perceive→recall(`inject(query)`)→decide(`brain.reason(promptFor(intent), ctx)`)→act (if `decision.action`, leave a `// P2+: router.dispatch` seam — no tools in P1)→log(`logSession`), surfacing `decision.reply` back to the intent's connection via the injected `send`. In `finally`, set `running=false` (fall genuinely idle). Inject the `BrainProvider` (StubBrain) so the seam is real. Export `enqueue`, `drain`.

    Create `daemon/src/heartbeat.ts`: `runHeartbeat()` calls `logHeartbeat()` and resolves — short-lived, no socket, no loop, used by the `--heartbeat` launchd job.

    Create `daemon/src/index.ts`: parse `process.argv` for `--heartbeat`; if present, `await runHeartbeat()` then `process.exit(0)`. Otherwise: run startup guards — `baselineIdentityHash()` (first run) + `readIdentityVerified()` (fails loud on tamper), and a finance assertion (`git -C <memdir> ls-files | grep -i finance` must be empty, else fail loud) — then `startIpc(onFrame)`, where `onFrame(frame, conn)` enqueues a `{source:'user', payload: frame.text, conn}` intent for an `utterance`, and replies `pong` for `ping`. Keep the process resident (the open socket keeps it alive); do NOT add a polling timer.

    Create `daemon/src/loop.test.ts`: inject a StubBrain + a fake `send`, enqueue a fake user intent against a temp memory dir, await drain; assert a `## Session` block was logged, a reply text was produced (StubBrain echo), `running===false`, and the queue is empty (idle). Create `daemon/test/heartbeat.test.ts`: run `runHeartbeat()` (or spawn `index.ts --heartbeat`) against a temp memory dir; assert `logs/{today}.md` contains a heartbeat line bearing today's date.
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npm run build && npx tsx --test src/loop.test.ts test/heartbeat.test.ts && npx tsx --test test/skeleton.e2e.test.ts</automated>
  </verify>
  <done>Event-driven serial runner drains one intent at a time and falls idle (no timer); one tick runs perceive→recall→decide→act→log and produces a reply; `--heartbeat` appends a dated line and exits; index.ts runs startup guards then starts ipc+loop; the Walking-Skeleton `skeleton.e2e.test.ts` is now GREEN. (CORE-01, CORE-02, CORE-03; VALIDATION: "One loop tick", "Falls idle", "Heartbeat writes a dated log entry".)</done>
</task>

<task type="auto">
  <name>Task 3: launchd plists (daemon login agent + timed heartbeat) + install runbook</name>
  <files>launchd/com.kernel.daemon.plist, launchd/com.kernel.heartbeat.plist, launchd/README.md</files>
  <action>
    Create `launchd/com.kernel.daemon.plist` per RESEARCH.md launchd patterns: `Label` = `com.kernel.daemon`; `ProgramArguments` = [ABSOLUTE node path, ABSOLUTE path to `daemon/dist/index.js`]; `RunAtLoad` = true; `KeepAlive` = true (restart on crash — "never clocks out"); `EnvironmentVariables` with an explicit `PATH` (`/usr/local/bin:/usr/bin:/bin`) and `KERNEL_MEMORY_DIR` (ABSOLUTE path to `kernel-memory/`); `StandardOutPath`/`StandardErrorPath` to `kernel-memory/logs/daemon.out.log` / `daemon.err.log`. Use clearly-marked `/ABSOLUTE/PATH/...` and `/Users/USERNAME/...` placeholders the runbook tells the owner to replace (launchd has a minimal env — node will not be on PATH unless the absolute path is used).

    Create `launchd/com.kernel.heartbeat.plist`: `Label` = `com.kernel.heartbeat`; `ProgramArguments` = [ABSOLUTE node path, ABSOLUTE `daemon/dist/index.js`, `--heartbeat`]; `StartCalendarInterval` = an array with one dict (e.g. `Minute = 0` for top-of-hour — owner-tunable cadence, RESEARCH.md A3); `EnvironmentVariables.KERNEL_MEMORY_DIR`; `StandardErrorPath` to `kernel-memory/logs/heartbeat.err.log`.

    Create `launchd/README.md`: the install/uninstall runbook (RESEARCH.md Runtime State Inventory) — how to fill placeholders (real node path via `which node`, real repo paths), then load with `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.kernel.daemon.plist` (and the heartbeat plist), kickstart the heartbeat for an immediate test (`launchctl kickstart -k gui/$(id -u)/com.kernel.heartbeat`), and unload with `launchctl bootout gui/$(id -u)/com.kernel.daemon`. Note: use `bootstrap`/`bootout` (NOT the deprecated `load`/`unload`); a label/path change requires `bootout` + re-`bootstrap`; copy the plists into `~/Library/LaunchAgents/` before bootstrapping.
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi && plutil -lint launchd/com.kernel.daemon.plist && plutil -lint launchd/com.kernel.heartbeat.plist && grep -q 'RunAtLoad' launchd/com.kernel.daemon.plist && grep -q 'StartCalendarInterval' launchd/com.kernel.heartbeat.plist && grep -q -- '--heartbeat' launchd/com.kernel.heartbeat.plist</automated>
  </verify>
  <done>Both plists are valid (`plutil -lint` passes); daemon plist has `RunAtLoad`+`KeepAlive`+absolute node path+explicit PATH+`KERNEL_MEMORY_DIR`; heartbeat plist has `StartCalendarInterval`+`--heartbeat`; README documents `bootstrap`/`bootout`/`kickstart`. (CORE-01, CORE-03; VALIDATION manual gate prerequisites.)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual launchd phase-gate — relaunch-at-login + heartbeat-fires verification</name>
  <what-built>The daemon, the UDS IPC endpoint, the event-driven loop, the append-only log, the heartbeat job, and two launchd plists. The automated suite (including the end-to-end `skeleton.e2e.test.ts`) is green. This checkpoint covers the two behaviors that cannot be automated in CI: launchd relaunch-at-login and the timed heartbeat firing on schedule (per 01-VALIDATION.md Manual-Only Verifications).</what-built>
  <how-to-verify>
    1. Fill the plist placeholders: `which node` for the absolute node path, and the absolute repo paths for `daemon/dist/index.js` and `kernel-memory/`. Build: `cd daemon && npm run build`.
    2. Copy both plists to `~/Library/LaunchAgents/`, then load:
       `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.kernel.daemon.plist`
       `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.kernel.heartbeat.plist`
    3. Confirm the daemon is alive: `launchctl print gui/$(id -u)/com.kernel.daemon` shows it running; the UDS exists at `~/Library/Application Support/Kernel/kernel.sock`.
    4. Attach test: run the tiny Node UDS test client (from `server.test.ts`'s helper, or a one-off) — confirm a `ready` frame and a `reply` to an `utterance` WITHOUT restarting the daemon; disconnect and re-attach to confirm re-attach works.
    5. Heartbeat test: `launchctl kickstart -k gui/$(id -u)/com.kernel.heartbeat`, then confirm a new dated `heartbeat ...` line appended to `kernel-memory/logs/{today}.md`.
    6. Relaunch test: log out and back in (or `launchctl bootout` then `bootstrap` to simulate), and confirm the daemon is running again and a fresh log entry appears.
    7. IDENTITY guard (optional but recommended): modify `kernel-memory/IDENTITY.md` out-of-band, restart the daemon, and confirm it FAILS LOUD on the hash mismatch (check `kernel-memory/logs/daemon.err.log`); then restore the file.
  </how-to-verify>
  <resume-signal>Type "approved" once relaunch-at-login and the heartbeat-fires checks pass, or describe what failed.</resume-signal>
  <verify>
    <human-check>launchd relaunches the daemon at login; the heartbeat plist appended a dated line; the Face stand-in attached over the UDS without a daemon restart and re-attached after disconnect.</human-check>
  </verify>
  <done>Owner has confirmed the daemon relaunches at login, the timed heartbeat writes a dated log entry, and the UDS attach/detach/re-attach works without a restart — ROADMAP success criteria 1, 3, and 5 verified on-device. (CORE-01, CORE-03, CORE-04.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (Face) → daemon UDS | Untrusted NDJSON frames cross here; a malformed frame must not crash the daemon. |
| launchd → node binary | launchd runs with a minimal environment; PATH/node resolution is the classic failure surface. |
| daemon startup → on-disk memory repo | Startup guards (IDENTITY hash, finance ls-files) gate before the daemon serves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | DoS | Malformed/partial IPC frame crashing the daemon | mitigate | `server.ts` buffers and splits on `\n` with partial-line carryover, `FrameSchema.safeParse` validates every line, parse/validation failures reply with an `error` frame and never throw out of the `data` handler (CORE-04; PITFALLS Pitfall 3; RESEARCH.md V5 Input Validation). |
| T-01-10 | Information Disclosure | Local socket exposure / remote attach | mitigate | UDS under `~/Library/Application Support/Kernel/` (filesystem-permission scoped), NOT a TCP port — no remote attach surface (CORE-04; RESEARCH.md V12). |
| T-01-11 | Tampering | Poisoned IDENTITY.md served at startup | mitigate | `index.ts` runs `readIdentityVerified()` at startup before serving; a hash mismatch fails loud (logged to `daemon.err.log`) and refuses to proceed (MEM-02 enforced at the daemon entry; manual tamper check in Task 4). |
| T-01-12 | Information Disclosure | finance/ accidentally tracked before backup exists | mitigate | `index.ts` startup `git ls-files \| grep finance` assertion fails loud if anything finance-pathed is tracked — the runtime arm of the MEM-06 defense laid in Plan 01 (RESEARCH.md MEM-06; §14 layer 4). |
| T-01-13 | DoS / Availability | Daemon dies silently under launchd ("works in terminal, fails under launchd") | mitigate | Absolute node path + explicit `EnvironmentVariables.PATH` + `KERNEL_MEMORY_DIR` in the plist; `StandardErrorPath` captures failures; Task 4 tests UNDER launchd, not just from the terminal (CORE-01; PITFALLS Pitfall 4/10). |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; the Phase-1 install set was verified at the Plan 01 Task 1 blocking-human gate. |
</threat_model>

<verification>
- `cd daemon && npm run build && npm test` — full suite GREEN, including `skeleton.e2e.test.ts`.
- `plutil -lint` passes on both plists; daemon plist has `RunAtLoad`+`KeepAlive`, heartbeat has `StartCalendarInterval`+`--heartbeat`.
- Manual (Task 4): launchd relaunches the daemon at login; the heartbeat appends a dated line; the UDS attach/detach/re-attach works without a restart.
</verification>

<success_criteria>
- Daemon persists and relaunches at login via launchd; event-driven loop runs and falls idle (CORE-01, CORE-02 → ROADMAP criterion 1).
- Timed launchd heartbeat appends a dated entry to the append-only logs/{date}.md (CORE-03, CORE-05 → ROADMAP criterion 3).
- UDS NDJSON IPC endpoint up; Face attaches without a daemon restart (CORE-04 → ROADMAP criterion 5).
- The full perceive→recall→decide→act→log tick runs end to end; `skeleton.e2e.test.ts` green (CORE-02 + the whole skeleton).
</success_criteria>

<output>
Create `.planning/phases/01-skeleton/01-03-SUMMARY.md` when done.
</output>
