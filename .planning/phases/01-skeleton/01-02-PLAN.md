---
phase: 01-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - daemon/src/memory/identity.ts
  - daemon/src/memory/identity.test.ts
  - daemon/src/memory/retrieve.ts
  - daemon/src/memory/retrieve.test.ts
  - daemon/src/memory/quarantine.ts
  - daemon/src/memory/quarantine.test.ts
  - daemon/src/memory/inject.ts
  - daemon/src/memory/inject.test.ts
autonomous: true
requirements: [MEM-02, MEM-03, MEM-04, MEM-05, PERS-01]
user_setup: []

must_haves:
  truths:
    - "IDENTITY.md passes a SHA-256 startup hash guard that fails loud on an out-of-band change; no daemon code path writes IDENTITY.md"
    - "Session injection assembles in priority order: IDENTITY.md → working-memory/current.md → reranked retrieved knowledge/tasks/projects"
    - "Injected context is hard-capped at 16384 chars; IDENTITY.md and current.md are never truncated; fail loud if they alone exceed the cap"
    - "Retrieval is keyword-only (no embeddings), reranked by keyword × recency (14-day half-life, 0.3 floor) × authority (longest path-prefix match)"
    - "External (source: 'external') content lands only in working-memory/quarantine/ via a single quarantine.ts write path and is never injected into privileged context (authority 0.0)"
  artifacts:
    - path: "daemon/src/memory/identity.ts"
      provides: "SHA-256 hash baseline + startup verify + write-path guard for IDENTITY.md"
      exports: ["readIdentityVerified", "computeIdentityHash", "baselineIdentityHash", "assertNotIdentityPath"]
    - path: "daemon/src/memory/retrieve.ts"
      provides: "Keyword candidate gather + authority×recency rerank"
      exports: ["retrieveAndRerank", "score", "tokenize"]
    - path: "daemon/src/memory/quarantine.ts"
      provides: "Single write path for external-sourced content into quarantine/"
      exports: ["quarantineWrite"]
    - path: "daemon/src/memory/inject.ts"
      provides: "Priority assembly + 16K cap enforcer (IDENTITY never dropped)"
      exports: ["inject"]
  key_links:
    - from: "daemon/src/memory/inject.ts"
      to: "daemon/src/memory/identity.ts"
      via: "calls readIdentityVerified() (hash-checked) as priority-1 content"
      pattern: "readIdentityVerified"
    - from: "daemon/src/memory/inject.ts"
      to: "daemon/src/memory/retrieve.ts"
      via: "fills remaining budget with retrieveAndRerank() results"
      pattern: "retrieveAndRerank"
    - from: "daemon/src/memory/retrieve.ts"
      to: "working-memory/quarantine/"
      via: "authority weight 0.0 so quarantine never enters privileged context"
      pattern: "quarantine"
---

<objective>
Build KERNEL's memory engine: the IDENTITY.md integrity guard (SHA-256 baseline + startup verify + write-path guard), keyword retrieval with authority×recency rerank (ported from the agentic-os `memory-config.json`, no embeddings), the single quarantine write path for external content, and the priority-order session injection under the hard 16K-char cap that never drops IDENTITY. This is the "recall" half of the perceive→recall→decide→act→log tick.

Purpose: Make ROADMAP success criterion 2 true (priority injection under cap + IDENTITY hash guard) and enforce MEM-05's quarantine no-promote rule at the code level (authority 0.0), not as a prompt instruction. These modules are consumed directly by the loop in Plan 03 and by the failing e2e test from Plan 01.

Output: Five tested memory modules; the e2e test's `inject()` assertion can now pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skeleton/01-RESEARCH.md
@.planning/phases/01-skeleton/01-VALIDATION.md
@.planning/phases/01-skeleton/01-01-SUMMARY.md
@daemon/src/memory/types.ts
@daemon/src/config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IDENTITY.md integrity guard + quarantine single-write path</name>
  <files>daemon/src/memory/identity.ts, daemon/src/memory/identity.test.ts, daemon/src/memory/quarantine.ts, daemon/src/memory/quarantine.test.ts</files>
  <behavior>
    - computeIdentityHash() returns the SHA-256 hex of IDENTITY.md's bytes (node:crypto, never hand-rolled).
    - baselineIdentityHash() records the current hash to a stored baseline (self/identity.hash under the memory dir) on first run.
    - readIdentityVerified() returns IDENTITY.md text when the current hash matches the baseline; when it differs it FAILS LOUD (throws a clear error / critical log) — it never silently returns or auto-re-baselines.
    - assertNotIdentityPath(target) throws if any write targets the IDENTITY.md path (the write-path guard); no daemon code path may edit IDENTITY.md.
    - quarantineWrite({text, origin}) writes external-sourced content ONLY under working-memory/quarantine/ (a timestamped/uuid filename), returns the written path, and refuses (throws) any target outside quarantine/.
  </behavior>
  <action>
    Create `daemon/src/memory/identity.ts` per RESEARCH.md MEM-02: use `node:crypto` `createHash('sha256')` over the IDENTITY.md bytes. `baselineIdentityHash()` writes the hex to `self/identity.hash` (resolved from `config` memory dir) if absent. `readIdentityVerified()` recomputes the hash, compares to the stored baseline, returns the file text on match, and on mismatch throws `IdentityIntegrityError` with a loud message (and emits a critical-level log line) — the only sanctioned way to change IDENTITY.md is a human edit followed by a human re-baseline, so there is NO auto-re-baseline path. Export `assertNotIdentityPath(target: string)` that throws if the resolved target equals the IDENTITY.md path — memory writers call it before writing.

    Create `daemon/src/memory/quarantine.ts`: `quarantineWrite(item: { text: string; origin?: string })` resolves `working-memory/quarantine/` under the memory dir, writes a new markdown file (filename = ISO-timestamp + short random) with YAML front-matter `source: external` + `origin`, and returns the path. It MUST refuse to write anywhere outside `quarantine/` (assert the resolved path is inside the quarantine dir). This is the single write path for `source === 'external'` content; Phase 1 has no caller that produces external content yet (Phase 2), but the seam + no-promoter rule exist now.

    Create `daemon/src/memory/identity.test.ts`: seed a temp memory dir, baseline, assert `readIdentityVerified()` returns the text on match; tamper the file out-of-band, assert `readIdentityVerified()` throws; assert `assertNotIdentityPath(<IDENTITY path>)` throws. Create `daemon/src/memory/quarantine.test.ts`: assert `quarantineWrite` lands a file under `quarantine/` with `source: external` front-matter and refuses an out-of-quarantine target.
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npx tsx --test src/memory/identity.test.ts src/memory/quarantine.test.ts</automated>
  </verify>
  <done>IDENTITY hash guard verifies on match and fails loud on tamper with no auto-re-baseline; write-path guard rejects IDENTITY writes; quarantineWrite is the single external-content path confined to quarantine/. Both tests pass. (MEM-02, MEM-05; VALIDATION: "IDENTITY.md injected + never auto-edited", "External content → quarantine, never promoted".)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keyword retrieval + authority×recency rerank (no embeddings)</name>
  <files>daemon/src/memory/retrieve.ts, daemon/src/memory/retrieve.test.ts</files>
  <behavior>
    - tokenize(s) returns a lowercase Set of [a-z0-9]+ tokens.
    - score(query, doc) = keywordOverlap × recencyMult × authority, where keywordOverlap = |query ∩ docTokens| / |query|, recencyMult = max(0.3, 0.5 ** (ageDays/14)), authority = weight of the longest matching path prefix.
    - authority_weights: knowledge/ = 1.5, working-memory/current.md = 1.0, tasks/ = 1.0, projects/ = 0.8, logs/ = 0.5, working-memory/quarantine/ = 0.0 (default 0.5 for unmatched).
    - retrieveAndRerank(query) gathers candidate docs from knowledge/, tasks/, projects/ (gray-matter front-matter split for status/priority/source), scores, and returns them sorted descending.
    - A higher-authority and/or more-recent doc outranks a stale/low-authority one; a quarantine-pathed doc scores 0 (never surfaced into privileged context).
  </behavior>
  <action>
    Create `daemon/src/memory/retrieve.ts` porting the agentic-os reranker from RESEARCH.md Code Examples: `HALF_LIFE = 14`, `FLOOR = 0.3`, and the `AUTH` map above. `tokenize` = lowercase `match(/[a-z0-9]+/g)` into a Set. `score(query: Set<string>, doc: { text: string; path: string; ageDays: number })` returns `keyword * recency * authority` where authority is the value for the longest matching key in `AUTH` (else 0.5). `retrieveAndRerank(query: string)` reads candidate files under `knowledge/`, `tasks/`, `projects/` (resolved from `config` memory dir), uses `gray-matter` to split front-matter (carrying `source`/`priority`/`status` and `path`), computes `ageDays` from file mtime, scores each, and returns a sorted (descending) array of `ContextItem`-shaped results (each carrying its `source` from front-matter, defaulting to `'self'`). Keyword-only — NO embeddings (16GB ceiling). Quarantine-pathed docs resolve to authority 0.0.

    Create `daemon/src/memory/retrieve.test.ts`: seed a temp memory dir with fixture docs (a recent high-authority `knowledge/` doc, a stale low-authority `logs/` doc, a `quarantine/` doc) all matching a query term; assert the knowledge doc outranks the stale log doc, and the quarantine doc scores 0 / is excluded.
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npx tsx --test src/memory/retrieve.test.ts</automated>
  </verify>
  <done>Keyword retrieval + authority×recency rerank works with no embeddings; higher-authority/recent docs outrank stale/low; quarantine authority is 0.0. Test passes. (MEM-04; VALIDATION: "Keyword retrieval + authority×recency rerank".)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Priority-order injection under the hard 16K cap (IDENTITY never dropped)</name>
  <files>daemon/src/memory/inject.ts, daemon/src/memory/inject.test.ts</files>
  <behavior>
    - inject(query) assembles, in order: hash-verified IDENTITY.md (priority 1, never truncated) → working-memory/current.md (priority 2, never truncated) → reranked retrieved items (priority 3) filling the remaining budget greedily.
    - The total returned string length is ≤ 16384 (INJECT_CAP); items that would overflow the remaining budget are skipped (not truncated mid-item).
    - inject() output BEGINS with IDENTITY.md content (IDENTITY is never dropped).
    - Items whose source === 'external' are excluded from privileged context (defense-in-depth alongside retrieve's 0.0 authority).
    - If IDENTITY + current alone exceed the cap, inject() fails loud (logs a warning / surfaces a metric) and still never drops IDENTITY.
  </behavior>
  <action>
    Create `daemon/src/memory/inject.ts` per RESEARCH.md Code Example + assembly algorithm: call `readIdentityVerified()` (throws/alerts on hash mismatch) for priority 1; read `working-memory/current.md` for priority 2; compute `budget = INJECT_CAP - len(identity) - len(current)`; if `fixed.length > INJECT_CAP` log a loud warning (fail-loud condition — IDENTITY+current must always fit) but never drop IDENTITY; call `retrieveAndRerank(query)` and greedily append items that fit the remaining budget, skipping `source === 'external'` items and any item that would overflow; join with `\n\n` and return. Import `INJECT_CAP` from `config`.

    Create `daemon/src/memory/inject.test.ts`: seed a temp memory dir with an IDENTITY.md (baselined), a current.md, and an oversized `knowledge/` corpus (enough to exceed the cap if naively concatenated); assert the result starts with the IDENTITY content, total length ≤ 16384, and at least one low-priority retrieved item was skipped (truncation happened on retrieved items, not IDENTITY). Add a case where IDENTITY+current exceed the cap and assert the loud-warning path fires while IDENTITY is still present.
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npx tsx --test src/memory/inject.test.ts</automated>
  </verify>
  <done>inject() assembles in priority order under a hard 16384-char cap; IDENTITY (hash-verified) and current.md are never truncated; retrieved overflow is skipped; external content is excluded; fail-loud fires when IDENTITY+current exceed the cap. Test passes. (MEM-02, MEM-03, PERS-01 injected; VALIDATION: "Priority order + 16K cap, IDENTITY never dropped".)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk IDENTITY.md → injected privileged context | A tampered IDENTITY would permanently steer behavior; the hash guard is the gate. |
| retrieved memory files → brain context | Retrieved content becomes privileged context; external/quarantine content must be excluded. |
| external write → durable memory | The single quarantine write path is the only landing zone for `source: external`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering / Persistence | IDENTITY.md silently mutated (memory poisoning of durable persona) | mitigate | `identity.ts` SHA-256 baseline + startup verify fails loud on any out-of-band change (no auto-re-baseline); `assertNotIdentityPath` write-path guard means no daemon code path can target IDENTITY.md (MEM-02; PITFALLS Pitfall 2/5). |
| T-01-06 | Tampering / EoP | External/quarantine content promoted into privileged injected context (indirect prompt injection) | mitigate | `retrieve.ts` assigns `working-memory/quarantine/` authority 0.0; `inject.ts` additionally skips `source === 'external'` items — code-level enforcement, not a prompt instruction; no promoter path exists in Phase 1 (MEM-05; PITFALLS Pitfall 1). |
| T-01-07 | DoS / Degradation | Context-budget blowout drops IDENTITY or the user instruction | mitigate | `inject.ts` enforces priority order, never truncates IDENTITY/current, skips (not truncates) overflowing retrieved items, and fails loud if IDENTITY+current alone exceed the 16384 cap (MEM-03; PITFALLS Pitfall 14). |
| T-01-08 | Tampering | Hand-rolled hash / weak integrity check | accept | Uses `node:crypto` SHA-256 (never hand-rolled per RESEARCH.md Security Domain V6); SHA-256 is sufficient for a local integrity baseline (not an adversarial-collision setting). |
</threat_model>

<verification>
- `cd daemon && npm test` — `identity.test.ts`, `quarantine.test.ts`, `retrieve.test.ts`, `inject.test.ts` all GREEN.
- `inject()` output starts with IDENTITY content and is ≤ 16384 chars; tampering IDENTITY.md makes `readIdentityVerified()` throw.
- A quarantine-pathed doc never appears in `inject()` output.
</verification>

<success_criteria>
- IDENTITY.md hash guard + write-path guard enforced; fails loud on tamper (MEM-02 → ROADMAP criterion 2).
- Priority-order injection under the hard 16K cap, IDENTITY never dropped (MEM-03 → ROADMAP criterion 2).
- Keyword retrieval + authority×recency rerank, no embeddings (MEM-04 → ROADMAP criterion 2).
- External content confined to quarantine/, authority 0.0, never injected (MEM-05 → ROADMAP criterion 5).
- PERS-01 persona content (IDENTITY) is the first thing injected every session (PERS-01 → ROADMAP criterion 2).
</success_criteria>

<output>
Create `.planning/phases/01-skeleton/01-02-SUMMARY.md` when done.
</output>
