---
phase: 01-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nvmrc
  - daemon/package.json
  - daemon/tsconfig.json
  - daemon/.env.example
  - daemon/.gitignore
  - daemon/src/config.ts
  - daemon/src/brain/BrainProvider.ts
  - daemon/src/brain/StubBrain.ts
  - daemon/src/brain/StubBrain.test.ts
  - daemon/src/memory/types.ts
  - daemon/src/safety/README.md
  - daemon/test/skeleton.e2e.test.ts
  - daemon/test/finance-ignore.test.ts
  - kernel-memory/.gitignore
  - kernel-memory/IDENTITY.md
  - kernel-memory/working-memory/current.md
  - kernel-memory/working-memory/quarantine/.gitkeep
  - kernel-memory/working-memory/reflections/.gitkeep
  - kernel-memory/knowledge/.gitkeep
  - kernel-memory/tasks/.gitkeep
  - kernel-memory/projects/registry.md
  - kernel-memory/logs/.gitkeep
  - kernel-memory/self/changelog.md
  - kernel-memory/self/metrics.md
autonomous: false
requirements: [MEM-01, MEM-06, BRAIN-01, MEM-05, PERS-01, PERS-02, PERS-03]
user_setup: []

must_haves:
  truths:
    - "Monorepo + daemon/ TypeScript ESM scaffold builds with tsc and runs the node:test harness via tsx"
    - "kernel-memory/ is its own git repo with the full spec §5 directory layout seeded"
    - "IDENTITY.md is seeded with Pravin's persona + the three voice rules (terse to Pravin / dynamic outward / clarify-don't-guess)"
    - "BrainProvider interface exists with Decision + ToolCall + BrainContext types; StubBrain satisfies it and returns a zod-valid Decision"
    - "ContextItem carries a source: 'user' | 'self' | 'external' provenance tag (the quarantine seam data shape)"
    - "finance/ is gitignored in kernel-memory/ and a git ls-files assertion proves nothing finance-pathed is tracked"
    - "A failing end-to-end test names the full tick the skeleton must satisfy (Walking Skeleton happy-path placeholder)"
  artifacts:
    - path: "daemon/package.json"
      provides: "Node 24 ESM project, pinned deps, build/dev/start/heartbeat/test scripts"
      contains: "\"type\": \"module\""
    - path: "daemon/tsconfig.json"
      provides: "NodeNext + strict + outDir dist/ build config"
      contains: "NodeNext"
    - path: "daemon/src/brain/BrainProvider.ts"
      provides: "BrainProvider interface + Decision + ToolCall + BrainContext types + DecisionSchema (zod)"
      exports: ["BrainProvider", "Decision", "ToolCall", "BrainContext", "DecisionSchema"]
    - path: "daemon/src/brain/StubBrain.ts"
      provides: "Deterministic in-process brain satisfying BrainProvider"
      exports: ["StubBrain"]
    - path: "daemon/src/memory/types.ts"
      provides: "Provenance + ContextItem + memory front-matter shapes"
      exports: ["Provenance", "ContextItem"]
    - path: "kernel-memory/IDENTITY.md"
      provides: "Seeded persona + voice rules; never auto-edited"
      contains: "Voice Rules"
      min_lines: 25
    - path: "kernel-memory/.gitignore"
      provides: "finance/ + sidecar ignore rules"
      contains: "finance/"
    - path: "daemon/test/skeleton.e2e.test.ts"
      provides: "Failing end-to-end happy-path test naming the full tick"
    - path: "daemon/test/finance-ignore.test.ts"
      provides: "MEM-06 git check-ignore + git ls-files assertion test"
  key_links:
    - from: "daemon/src/brain/StubBrain.ts"
      to: "daemon/src/brain/BrainProvider.ts"
      via: "implements BrainProvider, returns Decision"
      pattern: "implements\\s+BrainProvider"
    - from: "daemon/src/brain/StubBrain.test.ts"
      to: "DecisionSchema"
      via: "zod-validates the returned Decision"
      pattern: "DecisionSchema"
    - from: "kernel-memory/.gitignore"
      to: "kernel-memory/finance/"
      via: "gitignore rule + git ls-files assertion in finance-ignore.test.ts"
      pattern: "finance/"
---

<objective>
Lay the Walking Skeleton foundation for KERNEL: the monorepo + `daemon/` TypeScript-ESM scaffold with the `node:test`+`tsx` harness, the seeded `kernel-memory/` git repo (spec §5 layout, IDENTITY.md persona, finance gitignore), the `BrainProvider` interface + `StubBrain` (built FIRST per spec §6), the provenance `ContextItem` data shape, and a single FAILING end-to-end test that names the full perceive→recall→decide→act→log tick the rest of the phase must make pass.

Purpose: Establish the architecture's load-bearing contracts (brain swap-seam, memory layout, provenance tag, finance ignore) and the test harness before any behavior is wired, so Plans 02 and 03 implement against fixed contracts rather than exploring. This is the first vertical-slice task set under WALKING_SKELETON — after it, the failing e2e test enumerates exactly what "online" means.

Output: A buildable `daemon/` project, a seeded `kernel-memory/` repo, the brain seam, the provenance shape, the finance defense seam, and a red end-to-end test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/phases/01-skeleton/01-RESEARCH.md
@.planning/phases/01-skeleton/01-VALIDATION.md
@.planning/phases/01-skeleton/01-SKELETON.md
@docs/KERNEL_MASTER_BUILD_PROMPT.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Phase-1 package legitimacy before first install</name>
  <what-built>Nothing yet — this gate runs BEFORE any npm install. RESEARCH.md's Package Legitimacy Audit tagged all Phase-1 packages `[ASSUMED]` (slopcheck unavailable in the research environment). Per the legitimacy protocol's graceful-degradation rule, first-time installs are gated for a one-time human verification.</what-built>
  <how-to-verify>
    Confirm each package is the canonical, official package (open the registry pages; all are multi-year, very-high-download, official-repo, none declares a postinstall except tsx which is dev-only):
    1. https://www.npmjs.com/package/@anthropic-ai/sdk  (official Anthropic SDK — github.com/anthropics/anthropic-sdk-typescript)
    2. https://www.npmjs.com/package/pino  (github.com/pinojs/pino)
    3. https://www.npmjs.com/package/zod  (github.com/colinhacks/zod)
    4. https://www.npmjs.com/package/gray-matter  (github.com/jonschlinkert/gray-matter)
    5. https://www.npmjs.com/package/yaml  (github.com/eemeli/yaml)
    6. https://www.npmjs.com/package/typescript  (github.com/microsoft/TypeScript — pin 5.9.3, NOT 6.x)
    7. https://www.npmjs.com/package/tsx  (github.com/privatenumber/tsx — dev-only)
    8. https://www.npmjs.com/package/@types/node  (DefinitelyTyped — pin 24.x, NOT 26.x)
    9. https://www.npmjs.com/package/pino-pretty  (github.com/pinojs/pino-pretty — dev-only)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the pinned installs, or name any package to reject.</resume-signal>
  <verify>
    <human-check>All nine packages confirmed canonical/official at the registry URLs above.</human-check>
  </verify>
  <done>Owner has approved the Phase-1 package set; installs may proceed with exact pins.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the monorepo + daemon/ ESM/TS project + node:test harness (Wave 0)</name>
  <files>.nvmrc, daemon/package.json, daemon/tsconfig.json, daemon/.env.example, daemon/.gitignore, daemon/src/config.ts, daemon/src/safety/README.md</files>
  <action>
    Initialize the monorepo root git repo (`git init` at repo root if not already a repo). Create `.nvmrc` containing `24`.

    Create `daemon/package.json` with `"type": "module"`, `"name": "kernel-daemon"`, `"private": true`, and scripts: `build` (`tsc`), `dev` (`tsx watch src/index.ts`), `start` (`node dist/index.js`), `heartbeat` (`node dist/index.js --heartbeat`), `test` (`tsx --test "src/**/*.test.ts" "test/**/*.test.ts"`). Pin EXACT versions (no `^`): runtime deps `@anthropic-ai/sdk@0.105.0`, `pino@10.3.1`, `zod@4.4.3`, `gray-matter@4.0.3`, `yaml@2.9.0`; dev deps `typescript@5.9.3`, `tsx@4.22.4`, `@types/node@24.13.2`, `pino-pretty@13.1.3`. Do NOT take `latest` — TS `latest` is 6.x and `@types/node` `latest` is 26.x; the project pins TS 5.9.x and types 24.x (per STACK.md / RESEARCH.md Locked Decisions). Run the install with these exact pins.

    Create `daemon/tsconfig.json`: `module: "NodeNext"`, `moduleResolution: "NodeNext"`, `target: "ES2023"`, `strict: true`, `outDir: "dist"`, `rootDir: "src"`, `esModuleInterop: true`, `skipLibCheck: true`, `declaration: false`. Note for the executor: under NodeNext, intra-project TS imports MUST use `.js` extension specifiers (e.g. `import { StubBrain } from './brain/StubBrain.js'`).

    Create `daemon/.env.example` with `ANTHROPIC_API_KEY=` (referenced but UNUSED in Phase 1 — no API calls) and `KERNEL_MEMORY_DIR=` (path to the kernel-memory/ repo). Create `daemon/.gitignore` ignoring `node_modules/`, `dist/`, `.env`.

    Create `daemon/src/config.ts`: a zod-validated config object resolving `KERNEL_MEMORY_DIR` (default to the sibling `../kernel-memory` resolved absolutely), the UDS socket path (`~/Library/Application Support/Kernel/kernel.sock` via `os.homedir()`), and the injection cap constant `INJECT_CAP = 16384`. Export a typed `config` and the `INJECT_CAP` constant. Validate with zod and fail loud on a missing/invalid memory dir.

    Create `daemon/src/safety/README.md`: a one-paragraph stub documenting that `safety/` is the gate-chokepoint seam — empty in Phase 1, filled by Phase 2 (`gate.authorize`) and Phase 5 (full tiered gate + breaker). No code, no enforcement here (per RESEARCH.md scope).

    Verify `node:test` runs through tsx with a trivial smoke assertion (added in Task 3's test files); no external test framework is installed (Wave 0 requirement: `node:test` is built in).
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npm run build && test -f tsconfig.json && grep -q '"type": "module"' package.json && grep -q 'NodeNext' tsconfig.json && grep -q '"typescript": "5.9.3"' package.json</automated>
  </verify>
  <done>`daemon/` builds with `tsc` (empty/early build OK), `package.json` is ESM with exact pins (TS 5.9.3, @types/node 24.x), tsconfig uses NodeNext, config.ts validates and exports `config` + `INJECT_CAP`. (MEM-01 scaffold; Wave 0 harness.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BrainProvider interface + StubBrain + provenance ContextItem shape + zod Decision schema</name>
  <files>daemon/src/brain/BrainProvider.ts, daemon/src/brain/StubBrain.ts, daemon/src/brain/StubBrain.test.ts, daemon/src/memory/types.ts</files>
  <behavior>
    - StubBrain.reason(prompt, context) resolves to a Decision with a non-empty `thought` and a `reply` echoing the prompt; no `action` (no tools exist in Phase 1).
    - The returned Decision parses cleanly against DecisionSchema (zod) — `thought: string`, optional `action: {tool, args}`, optional `reply: string`.
    - ContextItem requires a `source` field typed as Provenance ('user' | 'self' | 'external') and a `text`; `origin` and `path` are optional.
    - DecisionSchema rejects a malformed Decision (e.g. missing `thought`) — assert `.safeParse` fails.
  </behavior>
  <action>
    Create `daemon/src/brain/BrainProvider.ts` exactly to spec §6 / RESEARCH.md: export `interface ToolCall { tool: string; args: Record<string, unknown> }`, `interface Decision { thought: string; action?: ToolCall; reply?: string }`, `interface BrainContext { identity: string; current: string; retrieved: ContextItem[] }` (import `ContextItem` from `../memory/types.js`), and `interface BrainProvider { reason(prompt: string, context: string): Promise<Decision> }`. Also export a zod `DecisionSchema` (and `ToolCallSchema`) mirroring the Decision shape so any brain's output is runtime-validated — built FIRST per BRAIN-01, before any implementation. Keep the spec-literal `reason(prompt, context: string)` signature; the structured `BrainContext` is available for future dual-LLM splitting (per RESEARCH.md note).

    Create `daemon/src/memory/types.ts`: export `type Provenance = 'user' | 'self' | 'external'` (with the user/self/external = Pravin/KERNEL/UNTRUSTED comments from RESEARCH.md), and `interface ContextItem { text: string; source: Provenance; origin?: string; path?: string }`. This is the MEM-05 quarantine-seam data shape — provenance set at the read/write site, carried into brain context.

    Create `daemon/src/brain/StubBrain.ts`: `export class StubBrain implements BrainProvider` with a deterministic, no-network `reason(prompt, _context)` returning `{ thought: \`stub considered: ${prompt.slice(0,80)}\`, reply: \`KERNEL skeleton online. (StubBrain echo) You said: ${prompt}\` }` (no `action`). Even the stub goes through `reason()` so the seam is real (RESEARCH.md anti-pattern: never hardcode the reply in the daemon).

    Create `daemon/src/brain/StubBrain.test.ts` (`node:test`): assert the four behaviors above, including that `DecisionSchema.parse(await new StubBrain().reason('hello','ctx'))` succeeds and that `DecisionSchema.safeParse({})` fails.
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npx tsx --test src/brain/StubBrain.test.ts</automated>
  </verify>
  <done>BrainProvider/Decision/ToolCall/BrainContext + DecisionSchema exist and are built before any impl; StubBrain implements the interface and returns a zod-valid Decision; ContextItem carries the `source` provenance tag; StubBrain.test.ts passes. (BRAIN-01, MEM-05 data shape; VALIDATION map: "BrainProvider satisfied by StubBrain", "ContextItem carries source: tag".)</done>
</task>

<task type="auto">
  <name>Task 4: Seed kernel-memory/ repo (spec §5 layout, IDENTITY persona+voice, finance gitignore) + finance assertion test + failing e2e test</name>
  <files>kernel-memory/.gitignore, kernel-memory/IDENTITY.md, kernel-memory/working-memory/current.md, kernel-memory/working-memory/quarantine/.gitkeep, kernel-memory/working-memory/reflections/.gitkeep, kernel-memory/knowledge/.gitkeep, kernel-memory/tasks/.gitkeep, kernel-memory/projects/registry.md, kernel-memory/logs/.gitkeep, kernel-memory/self/changelog.md, kernel-memory/self/metrics.md, daemon/test/finance-ignore.test.ts, daemon/test/skeleton.e2e.test.ts</files>
  <action>
    Initialize `kernel-memory/` as its OWN git repo (`git init` inside `kernel-memory/`, local-only; remote deferred to Phase 5 per RESEARCH.md Open Question 2). Create the full spec §5 / RESEARCH.md directory layout with `.gitkeep` files so empty dirs are tracked: `working-memory/quarantine/`, `working-memory/reflections/`, `knowledge/`, `tasks/`, `logs/`.

    Create `kernel-memory/.gitignore` with the broad finance defense (RESEARCH.md MEM-06): `finance/`, `**/finance/**`, `*.db-wal`, `*.db-shm`, `*.db-journal`, `finance/*.db`, and `.env`. `finance/` does NOT exist in Phase 1 — the ignore is pre-seeded so P4 can never accidentally commit it.

    Create `kernel-memory/IDENTITY.md` — the persona + voice seed (per spec §10 / PROJECT.md / RESEARCH.md), seeded once and NEVER auto-edited. It MUST contain a clearly-headed "Voice Rules" section encoding all three persona requirements verbatim in intent:
      - **To Pravin (PERS-01):** direct, terse, reporting-style; no bargaining once a task is stated; only vital details in notifications.
      - **Outward content (PERS-02):** dynamic register — warm for personal email, sharp for posts, formal for docs.
      - **Behaviour / clarify-don't-guess (PERS-03):** on a vocabulary mismatch (Pravin's wording doesn't match KERNEL's), elaborate/clarify rather than guess.
    Also include an Identity/persona header ("KERNEL is, in voice and judgment, a digital copy of Pravin Maurya; a foreman that never clocks out") and a short Owner section (Pravin Maurya). Use the recommended seed prose from RESEARCH.md as the base; persona facts are owner-confirmable. Target ≥25 lines.

    Create `kernel-memory/working-memory/current.md` — a minimal rolling-scratchpad seed (frozen-snapshot discipline, ~2.5K-char cap noted in a comment). Create `kernel-memory/projects/registry.md` with an empty header ("# Claude Code Project Registry" + a note it survives cold across sessions). Create `kernel-memory/self/changelog.md` and `kernel-memory/self/metrics.md` with empty headers.

    Create `daemon/test/finance-ignore.test.ts` (`node:test`, MEM-06): resolve the kernel-memory dir from config; assert `git -C <memdir> check-ignore finance/x.db` exits 0 (path is ignored) and assert `git -C <memdir> ls-files | grep -i finance` produces NO output (nothing finance-pathed is tracked). Use `node:child_process` execFileSync; treat a non-zero `grep` (no match) as PASS.

    Create `daemon/test/skeleton.e2e.test.ts` — the FAILING Walking-Skeleton happy-path test (red until Plans 02+03 land). It MUST be authored as the contract for the full tick and assert, end to end: (1) the IPC server listens on the UDS and a test client receives a `ready` frame on connect; (2) sending an `utterance` frame runs one loop tick and the client receives a `reply` frame whose text contains the StubBrain echo; (3) after the tick, `logs/{today}.md` contains a `## Session` block; (4) `inject()` output begins with IDENTITY.md content and total length ≤ 16384. Import the modules that Plans 02/03 will create (`../src/ipc/server.js`, `../src/loop.js`, `../src/memory/inject.js`); since they do not exist yet the test fails to import — that red state is intentional and documented in a top-of-file comment ("RED until 01-02 + 01-03; this is the Walking Skeleton acceptance contract"). Mark with a clear test name `skeleton: one tick perceive→recall→decide→act→log end to end`.
  </action>
  <verify>
    <automated>cd /Users/pravinmaurya/Documents/KernelAi/daemon && npx tsx --test test/finance-ignore.test.ts && test -f ../kernel-memory/IDENTITY.md && grep -qi 'Voice Rules' ../kernel-memory/IDENTITY.md && grep -q 'finance/' ../kernel-memory/.gitignore && git -C ../kernel-memory ls-files | grep -i finance | wc -l | grep -q '^ *0$'</automated>
  </verify>
  <done>kernel-memory/ is its own git repo with the full §5 layout; IDENTITY.md carries the three voice rules under a Voice Rules heading; finance is gitignored and the ls-files assertion passes; finance-ignore.test.ts is green; skeleton.e2e.test.ts exists and is RED (intentional, imports not-yet-built modules). (MEM-01, MEM-06, PERS-01/02/03; VALIDATION: "Memory layout exists", "finance/ gitignored + assertion", "IDENTITY seed encodes voice rules".)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | Package install pulls third-party code; postinstall scripts execute. |
| memory git repo → future GitHub backup | `kernel-memory/` will push to a private remote in Phase 5; anything tracked leaks. |
| external content → durable memory (future) | The provenance `source` tag is the seam that will keep external content out of privileged context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (`@anthropic-ai/sdk`, `pino`, `zod`, `gray-matter`, `yaml`, `typescript`, `tsx`, `@types/node`, `pino-pretty`) | mitigate | Task 1 blocking-human checkpoint verifies every `[ASSUMED]` package at its registry page before first install; exact version pins (no `^`) prevent silent drift; none declares a postinstall except dev-only `tsx`. |
| T-01-01 | Information Disclosure | `kernel-memory/finance/` leaking via git/backup | mitigate | Broad `finance/` + `**/finance/**` + SQLCipher-sidecar gitignore laid in `kernel-memory/.gitignore` now (before finance/ exists); `daemon/test/finance-ignore.test.ts` asserts `git ls-files` tracks nothing finance-pathed (MEM-06, the cheap fourth layer of §14 defense-in-depth). |
| T-01-02 | Tampering / EoP | Indirect prompt injection via future external content | mitigate | `ContextItem.source: 'user'\|'self'\|'external'` provenance tag exists from day one (`daemon/src/memory/types.ts`); the tag is the data shape the Phase-5 promotion gate depends on. Enforcement (authority 0.0 for quarantine) lands in Plan 02. |
| T-01-03 | Information Disclosure | Secrets (`ANTHROPIC_API_KEY`) committed to the memory repo | mitigate | Secrets live only in `daemon/.env` (gitignored via `daemon/.gitignore`), never in `kernel-memory/`; `.env.example` holds names only; `KERNEL_MEMORY_DIR` is a path, not a secret. |
| T-01-04 | Tampering | TS/types version drift to incompatible majors (TS 6.x / @types/node 26.x) | accept | Exact pins (TS 5.9.3, @types/node 24.13.2) in `package.json`; a future major bump is a deliberate, reviewed change, not an ambient risk. |
</threat_model>

<verification>
- `cd daemon && npm run build` succeeds.
- `cd daemon && npm test` runs the harness; `StubBrain.test.ts` and `finance-ignore.test.ts` are GREEN; `skeleton.e2e.test.ts` is RED (intentional — its modules land in Plans 02/03).
- `kernel-memory/` is its own git repo; `git -C kernel-memory ls-files | grep -i finance` is empty.
- `IDENTITY.md` contains the three voice rules; `BrainProvider.ts` exports the interface + `DecisionSchema` before any implementation.
</verification>

<success_criteria>
- daemon/ scaffold builds (Node 24 ESM, TS 5.9.3 pinned, NodeNext) and the node:test+tsx harness runs (MEM-01 scaffold; Wave 0).
- BrainProvider interface + Decision/ToolCall/BrainContext + DecisionSchema exist BEFORE any implementation; StubBrain satisfies them with a zod-valid Decision (BRAIN-01 → ROADMAP criterion 4).
- ContextItem carries the `source` provenance tag (MEM-05 data shape → ROADMAP criterion 4/5).
- kernel-memory/ seeded with the full §5 layout + IDENTITY persona/voice rules (MEM-01, PERS-01/02/03 → ROADMAP criterion 2).
- finance/ gitignored + ls-files assertion green (MEM-06 → ROADMAP criterion 5).
- The failing skeleton.e2e.test.ts names the full tick the phase must satisfy (Walking Skeleton happy-path).
</success_criteria>

<output>
Create `.planning/phases/01-skeleton/01-01-SUMMARY.md` when done.
</output>
