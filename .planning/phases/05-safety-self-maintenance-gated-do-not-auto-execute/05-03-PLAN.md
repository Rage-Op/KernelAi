---
phase: 05-safety-self-maintenance-gated-do-not-auto-execute
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - daemon/src/memory/consolidate.ts
  - daemon/src/memory/consolidate.test.ts
  - daemon/src/memory/prune.ts
  - daemon/src/memory/prune.test.ts
  - daemon/src/memory/backup.ts
  - daemon/src/memory/backup.test.ts
  - daemon/src/self/changelog.ts
  - daemon/src/self/changelog.test.ts
  - daemon/src/self/metrics.ts
  - daemon/src/self/metrics.test.ts
  - daemon/src/index.ts
  - daemon/test/launchd-jobs.test.ts
  - launchd/com.kernel.consolidation.plist
  - launchd/com.kernel.cleanup.plist
  - launchd/com.kernel.backup.plist
  - launchd/README.md
autonomous: false
user_setup:
  - service: github
    why: "MAINT-01 nightly backup pushes the kernel-memory repo to a private GitHub remote"
    env_vars: []
    dashboard_config:
      - task: "Create a private GitHub repo and add it as the kernel-memory remote (git remote add origin <private url>)"
        location: "GitHub -> New repository (private)"
      - task: "Add an SSH deploy key with push access for the kernel-memory repo"
        location: "GitHub repo -> Settings -> Deploy keys"
      - task: "Install the shipped pre-push hook into kernel-memory/.git/hooks/pre-push (the backup job refuses to push if it is absent)"
        location: "cp daemon/scripts/hooks/kernel-memory-pre-push.sh kernel-memory/.git/hooks/pre-push && chmod +x"
requirements: [MEM-07, MAINT-01, MAINT-02, MAINT-03]
must_haves:
  truths:
    - "A consolidation run distills logs -> reflections and promotes ONLY source:user/source:self durable facts to knowledge/"
    - "A consolidation run that processed ONLY external-sourced logs leaves knowledge/ AND IDENTITY.md byte-identical (no auto-promote, no auto-edit)"
    - "Cleanup prunes stale working-memory + old logs without touching IDENTITY.md, knowledge/, or finance/"
    - "Backup stages with explicit `git add <paths>` ONLY — the argv NEVER contains -A, -f, or '.'; finance/ is never staged"
    - "Backup refuses to push (fails loud) if the remote OR the pre-push hook is absent; a deliberately-staged fake finance file aborts the push"
    - "self/changelog.md and self/metrics.md are appended correctly and never target IDENTITY.md"
    - "The three launchd plists are valid plist XML and invoke `dist/index.js --consolidate|--cleanup|--backup`"
    - "index.ts dispatches --consolidate/--cleanup/--backup as short-lived modes that run and exit (mirroring --heartbeat)"
  artifacts:
    - path: "daemon/src/memory/consolidate.ts"
      provides: "Nightly consolidation: logs->reflections; promote ONLY source:user/self facts; never IDENTITY.md"
      exports: ["runConsolidation"]
    - path: "daemon/src/memory/prune.ts"
      provides: "Cleanup: prune stale working-memory + old logs"
      exports: ["runCleanup"]
    - path: "daemon/src/memory/backup.ts"
      provides: "Explicit-add git commit+push; finance excluded; fails loud without remote/hook"
      exports: ["runBackup"]
    - path: "daemon/src/self/changelog.ts"
      provides: "self/changelog.md writer"
      exports: ["appendChangelog"]
    - path: "daemon/src/self/metrics.ts"
      provides: "self/metrics.md writer"
      exports: ["writeMetrics"]
    - path: "launchd/com.kernel.backup.plist"
      provides: "Nightly backup launchd job (StartCalendarInterval, dist/index.js --backup)"
      contains: "--backup"
  key_links:
    - from: "daemon/src/index.ts"
      to: "daemon/src/memory/consolidate.ts"
      via: "argv.includes('--consolidate') -> runConsolidation(); process.exit(0)"
      pattern: "--consolidate"
    - from: "daemon/src/memory/backup.ts"
      to: "daemon/src/safety/leakguard.ts"
      via: "assertFinanceNotTracked before push (ls-files layer d)"
      pattern: "assertFinanceNotTracked"
    - from: "daemon/src/memory/consolidate.ts"
      to: "daemon/src/memory/types.ts"
      via: "filter promotion on source !== 'external'"
      pattern: "source"
---

<objective>
Build the nightly self-maintenance jobs as short-lived launchd modes mirroring the shipped
`--heartbeat` pattern: consolidation (logs -> reflections, promote ONLY source-vetted facts —
NEVER external/quarantine, NEVER IDENTITY.md), cleanup (prune stale working-memory + old logs),
backup (explicit `git add <paths>`, finance-excluded, fails loud without remote/hook + the
shipped pre-push hook + ls-files assertion), and the self/changelog.md + self/metrics.md writers,
plus the three `StartCalendarInterval` plists.

Purpose: MEM-07 (consolidation/prune, no privilege-escalation pump), MAINT-01 (finance-safe
GitHub backup), MAINT-02 (changelog + metrics), MAINT-03 (scheduled via launchd).

Output: `memory/consolidate.ts`, `memory/prune.ts`, `memory/backup.ts`, `self/changelog.ts`,
`self/metrics.ts`, edits to `index.ts`; the three plists + README; the Wave-0 tests
(consolidate/backup/changelog/metrics + plist lint). This plan ends with a human-verify checkpoint
for the owner-machine launchd + GitHub-remote setup (the only steps Claude cannot perform).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-safety-self-maintenance-gated-do-not-auto-execute/05-RESEARCH.md
@.planning/phases/05-safety-self-maintenance-gated-do-not-auto-execute/05-VALIDATION.md

# 05-01 delivered safety/test-helpers.ts (the temp-git-repo factory the backup test uses):
@.planning/phases/05-safety-self-maintenance-gated-do-not-auto-execute/05-01-SUMMARY.md

# The shipped patterns these jobs reuse:
@daemon/src/index.ts
@daemon/src/heartbeat.ts
@daemon/src/memory/log.ts
@daemon/src/memory/types.ts
@daemon/src/memory/quarantine.ts
@daemon/src/memory/retrieve.ts
@daemon/src/memory/identity.ts
@daemon/src/safety/leakguard.ts
@daemon/scripts/hooks/kernel-memory-pre-push.sh
@launchd/com.kernel.heartbeat.plist
@launchd/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consolidation (no-promote) + cleanup + self changelog/metrics (MEM-07, MAINT-02)</name>
  <files>daemon/src/memory/consolidate.ts, daemon/src/memory/consolidate.test.ts, daemon/src/memory/prune.ts, daemon/src/memory/prune.test.ts, daemon/src/self/changelog.ts, daemon/src/self/changelog.test.ts, daemon/src/self/metrics.ts, daemon/src/self/metrics.test.ts</files>
  <behavior>
    consolidate (MEM-07, Pitfall 4 — the privilege-escalation pump):
    - Test: logs containing source:user/source:self durable facts produce reflections AND promote those facts into knowledge/.
    - Test (CRITICAL INVARIANT): a run over logs containing ONLY source:external entries leaves knowledge/ AND IDENTITY.md BYTE-IDENTICAL (read the files' bytes before/after; assert equal). External facts may be SUMMARIZED for recall but NEVER promoted, and are marked "unverified, from <origin>".
    - Test: consolidate NEVER targets IDENTITY.md (reuse assertNotIdentityPath on every write target; a write that would hit IDENTITY.md throws).
    prune (MEM-07 cleanup):
    - Test: stale working-memory entries + old logs past a retention window are pruned; IDENTITY.md, knowledge/, and finance/ are untouched.
    changelog/metrics (MAINT-02):
    - Test: appendChangelog appends a dated entry to self/changelog.md (seeds a header if absent); writeMetrics writes self/metrics.md; neither targets IDENTITY.md (assertNotIdentityPath).
  </behavior>
  <action>
    Create `memory/consolidate.ts` exporting `runConsolidation(memoryDir?)`: read logs/ via the shipped log reader, group into reflections (gray-matter front-matter like quarantine.ts), and promote durable facts to knowledge/ — but FILTER promotion on `source !== 'external'` (Pitfall 4: promote ONLY source:user/source:self; external is summarized-for-recall with an "unverified, from <origin>" marker, never promoted). Call `assertNotIdentityPath` (identity.ts) before EVERY write so IDENTITY.md can never be a target. Reuse the shipped authority×recency rerank weights for "durable" selection. NEVER auto-edit IDENTITY.md or knowledge/ from external-tagged input.
    Create `memory/prune.ts` exporting `runCleanup(memoryDir?)`: prune stale working-memory/ entries + logs older than a retention window (explicit path operations; assertNotIdentityPath guard; finance/ and knowledge/ excluded by construction).
    Create `self/changelog.ts` (`appendChangelog(entry, memoryDir?)` -> append-only to self/changelog.md, seed a header if absent, assertNotIdentityPath) and `self/metrics.ts` (`writeMetrics(metrics, memoryDir?)` -> self/metrics.md, assertNotIdentityPath). Plain markdown; no finance amounts (V7).
    Write the four test files. Build the logs/ fixtures (including source:external entries) in a tmpdir so the real kernel-memory/ is never touched. The no-promote test reads knowledge/ + IDENTITY.md bytes before/after and asserts byte-identical.
  </action>
  <verify>
    <automated>cd daemon && npx tsx --test src/memory/consolidate.test.ts src/memory/prune.test.ts src/self/changelog.test.ts src/self/metrics.test.ts</automated>
  </verify>
  <done>All four test files green. The CRITICAL INVARIANT test passes: a consolidation run over ONLY external-sourced logs leaves knowledge/ and IDENTITY.md byte-identical. prune leaves IDENTITY/knowledge/finance untouched. changelog/metrics writers never target IDENTITY.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finance-safe GitHub backup (explicit-add, temp-remote dry-run) + index.ts job modes + launchd plists (MAINT-01, MAINT-03)</name>
  <files>daemon/src/memory/backup.ts, daemon/src/memory/backup.test.ts, daemon/src/index.ts, daemon/test/launchd-jobs.test.ts, launchd/com.kernel.consolidation.plist, launchd/com.kernel.cleanup.plist, launchd/com.kernel.backup.plist, launchd/README.md</files>
  <behavior>
    backup (MAINT-01, Pitfall 3/5 — finance leak):
    - Test (CRITICAL INVARIANT): the staged git argv uses explicit `git add <paths>` ONLY — assert the argv NEVER contains '-A', '-f', or '.'; finance/ is never among the staged paths.
    - Test (CRITICAL INVARIANT): a deliberately-staged fake `finance/test.txt` aborts the operation (assertFinanceNotTracked ls-files layer fires) — NO push happens.
    - Test: backup refuses to push (throws, fails loud) if no remote is configured OR the pre-push hook is absent (defense-in-depth).
    - Test: the WHOLE flow runs against a TEMP git repo + a TEMP bare "remote" (the temp-git-repo factory from safety/test-helpers.ts) — NEVER a real GitHub remote, NEVER a real push.
    index modes (MAINT-03): argv --consolidate/--cleanup/--backup each run the job and process.exit(0) (mirror --heartbeat); default resident mode unchanged.
    plists (MAINT-03): each is valid plist XML referencing `dist/index.js --<job>` with StartCalendarInterval.
  </behavior>
  <action>
    Create `memory/backup.ts` exporting `runBackup(memoryDir?, deps?)`: stage with EXPLICIT paths only via `git -C <repo> add <path1> <path2> ...` (the tracked memory paths — IDENTITY.md, working-memory/, knowledge/, tasks/, projects/, logs/, self/changelog.md, self/metrics.md) — NEVER `-A`/`-f`/`.` (Pitfall 3). Before push: run `assertFinanceNotTracked(memoryDir)` (leakguard.ts, ls-files layer d) AND verify the pre-push hook exists at `<repo>/.git/hooks/pre-push` AND a remote is configured (`git remote get-url origin`) — any absent -> THROW (fail loud, MAINT-01 / RESEARCH Env Availability). Then `git commit` + `git push`. Shell git via `execFileSync` (the leakguard.ts convention — zero-dep, NOT simple-git). Inject the git runner + paths so tests run against a temp repo and a temp bare remote (no real push). self/spend-ledger.json and self/audit-log are gitignored (add them to kernel-memory/.gitignore — they are machine-local runtime state per the locked decision) so they are never staged.
    Edit `index.ts`: add `if (argv.includes('--consolidate')) { await runConsolidation(); process.exit(0); }` and the same for `--cleanup` -> runCleanup and `--backup` -> runBackup, placed alongside the shipped `--heartbeat` branch (short-lived, exit). Default resident mode untouched (SAFE-07 behavior-preserving).
    Create the three plists by copying `com.kernel.heartbeat.plist` and changing Label, ProgramArguments (`dist/index.js --consolidate|--cleanup|--backup`), and StartCalendarInterval to staggered nightly times (e.g. consolidation 03:00, cleanup 03:30, backup 04:00). Keep the `/ABSOLUTE/PATH/...` placeholders + EnvironmentVariables (PATH + KERNEL_MEMORY_DIR) exactly as the heartbeat plist. Update `launchd/README.md` with the three new jobs + the `launchctl bootstrap` step + the GitHub-remote/deploy-key/hook-install owner steps.
    Create `test/launchd-jobs.test.ts`: parse each new plist as XML (lint, NOT execution) and assert it contains the correct `--<job>` ProgramArgument + a StartCalendarInterval. Write `backup.test.ts` using the temp-git-repo + temp-bare-remote factory.
  </action>
  <verify>
    <automated>cd daemon && npx tsx --test src/memory/backup.test.ts test/launchd-jobs.test.ts && npm test</automated>
  </verify>
  <done>backup.test.ts green: explicit-add argv (no -A/-f/.), staged fake finance aborts, absent remote/hook fails loud, whole flow on a temp repo + temp bare remote (no real push). launchd-jobs.test.ts green: 3 plists valid XML referencing the correct --<job>. index.ts has the three short-lived modes. `npm test` full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The three maintenance jobs (consolidation/cleanup/backup), their launchd plists, and the finance-safe backup with explicit-add + ls-files assertion + pre-push-hook requirement. All irreversible paths are tested with temp repos/mocks — NO real push has occurred. This checkpoint covers the owner-machine steps Claude literally cannot perform (RESEARCH Manual owner checks / Runtime State Inventory).</what-built>
  <how-to-verify>
    1. Build: `cd daemon && npm run build` (the plists run dist/index.js, not src/).
    2. Edit the three plists' `/ABSOLUTE/PATH/...` placeholders (node path, daemon/dist/index.js, KERNEL_MEMORY_DIR) per launchd/README.md.
    3. Load them: `launchctl bootstrap gui/$(id -u) launchd/com.kernel.consolidation.plist` (and cleanup, backup). Confirm a nightly run fires and writes reflections/changelog (check kernel-memory/logs + self/).
    4. GitHub backup setup: create a PRIVATE GitHub repo; `git -C kernel-memory remote add origin <private url>`; add an SSH deploy key with push access; install the pre-push hook: `cp daemon/scripts/hooks/kernel-memory-pre-push.sh kernel-memory/.git/hooks/pre-push && chmod +x kernel-memory/.git/hooks/pre-push`.
    5. Trigger one real backup: `node daemon/dist/index.js --backup`. Confirm it pushes successfully AND on the remote `git ls-files | grep -i finance` is EMPTY (finance never left the machine).
    6. If no remote/hook is configured yet, confirm the backup job FAILS LOUD (refuses to push) rather than leaking — that is the designed behavior.
  </how-to-verify>
  <resume-signal>Type "approved" (jobs load + a backup push succeeds with finance absent) or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logs/ (may contain external content) -> knowledge/ | Consolidation must never promote external-tagged facts (privilege-escalation pump) |
| consolidation/cleanup writers -> IDENTITY.md | No automated path may ever edit IDENTITY.md |
| backup -> remote GitHub | finance/ bytes must NEVER leave the machine; explicit-add + 4-layer stack |
| launchd -> dist/index.js | Scheduled short-lived jobs run on the OS PATH (absolute node path required) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | consolidate.ts promotion | mitigate | Promote ONLY source:user/source:self; external summarized-not-promoted; assertNotIdentityPath on every write; byte-identical invariant test (Pitfall 4) |
| T-05-13 | Information Disclosure | backup.ts | mitigate | Explicit `git add <paths>` (never -A/-f/.); assertFinanceNotTracked before push; refuse-to-push if hook/remote absent; staged-fake-finance abort test (Pitfall 3/5) |
| T-05-14 | Tampering | self/* + consolidate writers | mitigate | assertNotIdentityPath guard on every write target; IDENTITY.md hash baseline (shipped) catches any out-of-band edit |
| T-05-15 | Denial of Service | launchd job modes | accept | Jobs are short-lived (spawn->work->exit, like --heartbeat); a slept Mac runs them at next wake; 16GB ceiling respected |
| T-05-16 | Information Disclosure | spend-ledger.json / audit-log in backup | mitigate | Both gitignored (machine-local runtime state per locked decision); never staged by the explicit-add path |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none); nothing to slopcheck |
</threat_model>

<verification>
- `cd daemon && npm test` fully green (P1-P4 + 05-01 + this plan).
- The critical-invariant tests are present + green: consolidation no-promote (external-only run leaves knowledge/+IDENTITY.md byte-identical), backup explicit-add (argv never -A/-f), staged-fake-finance aborts, no real push (temp repo + temp bare remote).
- No new entries in `daemon/package.json` dependencies.
- The three plists are valid XML and reference the correct `--<job>` modes.
</verification>

<success_criteria>
- MEM-07: nightly consolidation distills logs->reflections + promotes ONLY source-vetted facts; cleanup prunes stale working-memory/logs; external facts NEVER promoted; IDENTITY.md NEVER auto-edited.
- MAINT-01: backup commits+pushes the memory repo to private GitHub via explicit `git add <paths>` (never -A), never including finance/; fails loud without remote/hook.
- MAINT-02: self/changelog.md + self/metrics.md maintained.
- MAINT-03: consolidation/cleanup/backup run on schedule via launchd (3 plists; --consolidate/--cleanup/--backup short-lived modes).
</success_criteria>

<output>
Create `.planning/phases/05-safety-self-maintenance-gated-do-not-auto-execute/05-03-SUMMARY.md` when done.
</output>
