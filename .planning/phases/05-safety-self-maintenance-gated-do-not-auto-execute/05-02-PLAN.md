---
phase: 05-safety-self-maintenance-gated-do-not-auto-execute
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - daemon/src/tools/claude-code.ts
  - daemon/src/tools/claude-code.test.ts
  - daemon/src/planner/ladder.ts
  - daemon/src/planner/ladder.test.ts
autonomous: true
requirements: [SAFE-05, SAFE-06]
must_haves:
  truths:
    - "A Claude Code session runs with disallowedTools Red deny rules (Bash(rm *), Bash(*install*), Bash(*git push*), Bash(sudo *), Bash(rmdir *)) carried in the argv, enforced even under bypass"
    - "Each permission_denials entry in the final result event maps to a ToolCall and RE-ENTERS gate.authorize (origin:'self') so a Red action routes to the SAME breaker and never auto-runs"
    - "The Red re-entry is gated by the breaker (05-01), not auto-executed — the mock dispatch is called for each denial"
    - "The obstacle ladder runs try -> replan -> decompose -> retry-with-backoff -> escalate-with-SPECIFIC-recommendation; never a vague 'I'm stuck'"
    - "A Red-tier gate verdict makes the ladder SKIP straight to escalate immediately (Red gates never enter the retry ladder)"
  artifacts:
    - path: "daemon/src/planner/ladder.ts"
      provides: "Obstacle ladder state machine wrapping dispatch (try->replan->decompose->backoff->escalate)"
      exports: ["runLadder", "LadderDeps", "LadderOutcome"]
    - path: "daemon/src/tools/claude-code.ts"
      provides: "CC Red shim: disallowedTools deny rules + permission_denials -> gate re-entry"
      contains: "disallowedTools"
  key_links:
    - from: "daemon/src/tools/claude-code.ts"
      to: "daemon/src/tools/registry.ts"
      via: "mapDenialToToolCall -> dispatch (re-enters gate -> breaker)"
      pattern: "permission_denials"
    - from: "daemon/src/planner/ladder.ts"
      to: "daemon/src/tools/registry.ts"
      via: "dispatch with Red-skip-to-escalate on a gated/deny verdict"
      pattern: "escalate"
---

<objective>
Build the two control structures that ride the activated breaker: (1) the Claude Code Red
re-submission shim — `claude` runs with `disallowedTools` scoped deny rules (bypass-proof) and
each `permission_denials` entry RE-ENTERS `gate.authorize` (origin:'self') so a mid-session
`rm -rf`/purchase routes to the SAME breaker from 05-01 and never auto-runs; and (2) the obstacle
planner ladder — a state machine wrapping dispatch with try -> replan -> decompose ->
retry-with-backoff -> escalate-with-a-specific-recommendation, where a Red gate skips straight to
escalate.

Purpose: SAFE-05 (Red gating inside Claude Code via the re-submission shim, re-entering the same
breaker with TOCTOU defense) and SAFE-06 (no vague "I'm stuck"; Red gates skip the ladder).

Output: edits to `tools/claude-code.ts` (+ test); new `planner/ladder.ts` (+ test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-safety-self-maintenance-gated-do-not-auto-execute/05-RESEARCH.md
@.planning/phases/05-safety-self-maintenance-gated-do-not-auto-execute/05-VALIDATION.md

# 05-01 delivered the breaker + gated arm the shim re-enters:
@.planning/phases/05-safety-self-maintenance-gated-do-not-auto-execute/05-01-SUMMARY.md

# The shipped CC bridge + the dispatch path the shim/ladder re-enter:
@daemon/src/tools/claude-code.ts
@daemon/src/tools/claude-code.test.ts
@daemon/src/tools/registry.ts
@daemon/src/brain/BrainProvider.ts
@daemon/src/loop.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Nothing yet — this checkpoint confirms the exact installed `claude` CLI permission-flag spelling BEFORE the shim argv is written (RESEARCH Open Q4 / A6: the disallowedTools flag name is fast-moving and must be confirmed at plan/execution time, NOT assumed).</what-built>
  <how-to-verify>
    1. Run: `claude --help 2>&1 | grep -i -E 'disallow|permission|allowed'`
    2. Confirm the exact spelling of the deny-rules flag (`--disallowedTools` vs `--disallowed-tools`) and that `permission_denials` (or the equivalent) surfaces in the stream-json final result event at this installed version.
    3. If `claude` is absent (exit 127): the shim is built absent-tolerant (mocked runner in tests); confirm "absent — build against the documented contract, tests mock the stream" and proceed.
    Report the confirmed flag spelling so Task 1 uses the correct argv.
  </how-to-verify>
  <resume-signal>Type the confirmed flag spelling (e.g. "--disallowedTools") or "absent — use documented contract".</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Claude Code Red shim — disallowedTools deny rules + permission_denials -> gate re-entry (SAFE-05)</name>
  <files>daemon/src/tools/claude-code.ts, daemon/src/tools/claude-code.test.ts</files>
  <behavior>
    - Test: argvFor(prompt) now includes the confirmed disallowedTools flag carrying RED_DENY = [Bash(rm *), Bash(rmdir *), Bash(*install*), Bash(*git push*), Bash(sudo *)] (assert the argv contains the flag + each deny rule). The Green/Yellow read-only fence (--permission-mode dontAsk --allowedTools Read --bare) is RETAINED.
    - Test: a mocked stream-json final result event carrying `permission_denials:[{tool:'Bash', input:{command:'rm -rf /tmp/x'}}]` causes runSession to map each denial to a ToolCall via `mapDenialToToolCall` (e.g. {tool:'shell', args:{op:'rm -rf', path:'/tmp/x'}, origin:'self'}) and call the injected `dispatch` once per denial.
    - Test: the re-entered ToolCall has `origin:'self'` (it came from KERNEL's own sub-contractor, NOT external content — so it is NOT hard-blocked, it is GATED by the breaker).
    - Test: the re-entry does NOT auto-run — `dispatch` is called (which in 05-01 routes a Red verdict to the breaker); the shim itself never executes the destructive op (RESEARCH Pitfall 6).
    - Test: NO real `claude` is spawned (runner mocked via __setRunnerForTest); NO real rm/push.
  </behavior>
  <action>
    Edit `tools/claude-code.ts`: add `RED_DENY` (the deny-rule list, top-level const) and extend `argvFor` to append the confirmed deny-rules flag + `RED_DENY.join(',')` while KEEPING the shipped Green/Yellow read-only fence. Parse the stream-json final `result` event for `permission_denials[]` (extend `StreamEvent` to read it; a malformed/absent field is tolerated per the shipped discipline). Add `mapDenialToToolCall(denial): ToolCall` that builds a `{tool, args:{op,...}, origin:'self'}` from each denial. Inject a `dispatch` seam into `RunSessionDeps` (`dispatch?: (call:ToolCall)=>Promise<ToolResult>`, defaulting to the real `registry.dispatch`) and, for each denial, `await deps.dispatch(reentry)` so the Red action RE-ENTERS the gate -> breaker (05-01) and is owner-gated, never auto-run. Emit a transcript line noting the re-gated action. Keep the module absent-tolerant; the runner stays injectable so tests never spawn `claude`.
  </action>
  <verify>
    <automated>cd daemon && npx tsx --test src/tools/claude-code.test.ts</automated>
  </verify>
  <done>claude-code.test.ts extended + green: argv carries the disallowedTools deny rules; a mocked permission_denials stream maps to ToolCalls (origin:'self') that re-enter the injected dispatch once each and never auto-run. No real claude spawn, no real irreversible op.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Obstacle planner ladder — try->replan->decompose->backoff->escalate; Red skips (SAFE-06)</name>
  <files>daemon/src/planner/ladder.ts, daemon/src/planner/ladder.test.ts</files>
  <behavior>
    - Test: a call that succeeds on the first try returns success with no escalation (ladder short-circuits at TRY).
    - Test: injected transient failures drive the ladder TRY -> REPLAN (brain proposes approach B) -> DECOMPOSE (split into sub-steps) -> RETRY-WITH-BACKOFF (the injected clock advances; backoff intervals grow) -> ESCALATE with a SPECIFIC recommendation in the shape "X blocked by Y; I recommend Z. Approve?" (assert the escalation text is specific, never the vague "I'm stuck").
    - Test: a dispatch that returns a Red `gated`/`deny` verdict makes the ladder SKIP straight to escalate immediately (no retry, no backoff) — only Red gates skip the ladder (SAFE-06 / RESEARCH State-of-the-Art).
    - Test: the brain replan/recommend step is INJECTED (mock) so no real cloud call; backoff uses the injected clock (no real timers).
  </behavior>
  <action>
    Create `planner/ladder.ts`: `runLadder(call, deps:LadderDeps): Promise<LadderOutcome>` where `LadderDeps = { dispatch, brain (replan/recommend seam), clock, maxRetries, backoffBaseMs }`. State machine: TRY dispatch; on a clean failure (not a Red gate) -> REPLAN (ask the brain for approach B) -> DECOMPOSE (brain splits into sub-steps, run each through dispatch) -> RETRY with exponential backoff via `deps.clock.sleep` (advanceable in tests) up to maxRetries -> ESCALATE building a SPECIFIC recommendation string ("`<goal>` blocked by `<reason>`; I recommend `<brain recommendation>`. Approve?"). CRITICAL: if a dispatch verdict is Red-gated/denied (the result carries the gated/deny escalation), `runLadder` returns the escalation IMMEDIATELY — Red gates skip the ladder (they belong to the breaker, not the retry loop). The ladder wraps dispatch; it NEVER bypasses the gate (BRAIN-06 invariant preserved). Reuse the shipped `ClaudeBrain` only via the injected brain seam (versioned model id, never *-latest — CLAUDE.md). This is a NEW control structure; do NOT modify loop.ts's existing inline escalation in this task beyond what 05-01 already wired (the loop may call runLadder for non-Red obstacles, but that integration is optional polish — the unit-tested state machine is the deliverable).
    Write `planner/ladder.test.ts` with injected failures, an injected brain (mock replan/recommend), and the fake clock from `safety/test-helpers.ts` — assert every rung and the Red-skip.
  </action>
  <verify>
    <automated>cd daemon && npx tsx --test src/planner/ladder.test.ts && npm test</automated>
  </verify>
  <done>ladder.test.ts green: first-try success short-circuits; injected failures climb try->replan->decompose->backoff->escalate with a SPECIFIC recommendation; a Red gate skips straight to escalate. `npm test` full suite green (no regression). No real brain call, no real timer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code session -> KERNEL gate | A CC sub-session has its own tool surface; its Red actions must re-enter KERNEL's chokepoint, not run ambiently |
| CC stdout (stream-json) -> daemon | The result event's permission_denials is the signal KERNEL acts on; a malformed line must not crash the loop |
| ladder -> dispatch | The ladder must wrap, never bypass, the gate (BRAIN-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Elevation | claude-code.ts CC Red gating | mitigate | disallowedTools scoped deny rules enforced even under bypass + permission_denials re-enter the breaker (origin:'self'); verified by the mocked-denial re-entry test (Pitfall 6 / SAFE-05) |
| T-05-09 | Tampering | claude-code.ts stream parse | mitigate | Absent/malformed permission_denials tolerated (shipped discipline) — a bad line is skipped, never thrown across the loop |
| T-05-10 | Elevation | ladder.ts | mitigate | runLadder wraps dispatch; a Red gate skips straight to escalate (never retried/auto-run); BRAIN-06 manual-loop invariant preserved |
| T-05-11 | Spoofing | mapDenialToToolCall origin | accept | Re-entered actions are origin:'self' (KERNEL's own sub-contractor), correctly GATED by the breaker rather than external-hard-blocked; CC cannot inject external-origin actions because the deny rules fire before any external instruction executes |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none); nothing to slopcheck |
</threat_model>

<verification>
- `cd daemon && npm test` fully green (P1-P4 + 05-01 + this plan).
- SAFE-05 critical-invariant test present + green: a mocked CC Red action (rm -rf/purchase) re-enters the gate->breaker via permission_denials and does NOT auto-run.
- SAFE-06 critical-invariant test present + green: a Red gate skips the ladder and escalates immediately; non-Red obstacles climb the full ladder to a specific recommendation.
- No new entries in `daemon/package.json` dependencies.
</verification>

<success_criteria>
- SAFE-05: CC runs with disallowedTools Red deny rules (bypass-proof); permission_denials map to ToolCalls (origin:'self') that re-enter the SAME breaker (05-01) and never auto-run; TOCTOU defense lives in the breaker the re-entry reaches.
- SAFE-06: the ladder runs try->replan->decompose->retry-with-backoff->escalate with a SPECIFIC recommendation; only Red gates skip the ladder.
</success_criteria>

<output>
Create `.planning/phases/05-safety-self-maintenance-gated-do-not-auto-execute/05-02-SUMMARY.md` when done.
</output>
